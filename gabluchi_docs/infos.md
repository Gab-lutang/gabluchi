# GabLuchi — research dossier (gathered 2026-09-19; rev 11 roll-up 2026-09-20)

## 1. Mugi live stack (v5.2.0) — full backend RE (rev 7 deep pass, plugin.zip + SPA in-memory)

**Distribution (as shipped /download/plugin.zip):** `Mugi/plugin.json` (name Mugi, **version 5.2.0**, author Shaninago, backendType lua, webkitApiVersion 2.0.0) + `Mugi/backend/main.lua` (6,667 lines — the whole backend) + `backend/types/*.lua` (cjson/datetime/fs/http/logger/millennium/utils) + `locales/*.json` (13 langs) + `Mugi/.millennium/Dist/{index.js, webkit.js, index.js.before-key-menu-20260919-182901.bak}`. The `.bak` is dated **today** — a pre-"key-menu" backup of the live bundle shipped in the same zip (active dev). Client components: `Steam\millennium\plugins\Mugi\` + `Steam\OpenSteamTool.dll|dwmapi.dll|xinput1_4.dll` + `opensteamtool.toml` + `Steam\cloud_redirect.dll` + `MugiLauncher.exe` (Qt6).

**Core constants:**
- `VERSION="5.2.0"`; `API_BASE_URL=https://mugi.store/api/plugin`; `ONLINE_API_BASE_URL=https://mugi.store`; `PLUGIN_UPDATE_URL=/download/plugin.zip`.
- **Dev/local backend override:** env `MUGI_LOCAL_BACKEND=1` → `127.0.0.1:4000/api/plugin`; also auto-detects a local `my-react-app` dev backend on `127.0.0.1:4000`.
- `API_KEY_ALLOWED_PREFIXES = {mtag_, mugi_, premium_, epic_, nitro_}`; `online-fix.me` RAR password; `HTTP_TIMEOUT=8`; `BACKEND_DOWN_COOLDOWN=20`; daily-limit codes table.
- **`server_route` host-pin system:** `Steam\config\manilua\api_server.txt` pins `mugi.store` or `mugiplugin.com`; `apply(host)` rewrites every base URL + update URL + resets cooldown; `sync()` runs before every backend call. **The entire backend is host-redirectable via one config file** (supply-chain/MITM surface). **Rev 8 cross-check:** Mugi's own unreleased plugin changelog (v1.2.0) confirms the paired UX — *"Automatically switch to mugiplugin.com when mugi.store cannot connect, remember the selected server, and choose either server in Settings"* + *"new server to fix server unreachable"*. Two live hosts + user-switchable default = doubled backend-swap surface. It also independently confirms: MugiPlay avatar frames (gold Premium / red-fire Epic) prove the social feed, `requiredPluginVersion` ("detects plugins older than 5.1.2"), one-click health repair with Steam restart, and Lua-only downloads (`dtype=1`).

**Anti-tamper header set (fully forgeable — no real HWID):**
- `X-Steam-PID` = **random 2000–32000** (first call); `X-Millennium-Version: 3.1.0`; `X-Plugin-Checksum` = hex of fixed key string; `X-Device-Fingerprint = simple_hex_hash(COMPUTERNAME.."-"..USERNAME, 64)`; `X-Process-Hash`/`X-Memory-Proof` = hex of `timestamp-process-data-<pid>` / `timestamp-memory-proof-<pid>` (hash 32); `X-Plugin-Timestamp` = ms timestamp; UA `manilua-plugin/3.1.0 (Millennium)`; `X-Online-Key` header when a key is set. `simple_hex_hash` = djb2(5381,×33) + FNV1a(2166136261,×37) blend, 8-hex-groups hash expansion.

**Key lifecycle (survives plugin updates):**
- Persistent store `Steam\config\manilua\{online_key.txt, api_key.txt}` (legacy copies in the volatile backend dir auto-migrate). `on_load` reads both, syncs whichever exists, writes both.
- `SetAPIKey`: prefix-validate → `verify_online_key` → `POST plugin/online-files/authenticate {key}`; allows save on `connection_error`/daily-limit but **rejects on wrong/banned/hwid_mismatch/device_blocked**. Response carries `record` + **`newKey` (server-side rotation) and `fallbackKey`** — plugin silently swaps stored keys and re-checks. Failures wipe `_online_key=stored nil`.
- Full RPC surface (~100 `local function`s in main.lua): `invalidate/ensure/safe` request core (`safe_get/post/put/delete`, `invoke_raw_request`, `friendly_http_error`), `verify_online_key`/`_check_online_key`/`process_plugin_update` (auth + update-required + fallback/rotation + daily-limit + http_busy classification), settings (`read/write_plugin_settings`, `SetPluginSetting`, `GetPluginSetting`), keys (`SetAPIKey`, `GetAPIKeyStatus`, `SetOnlineKey`, `ClearOnlineKey`, `GetOnlineAccessStatus`, `GetOnlineKeyInfo`, `ValidateOnlineKey`), downloads (`addViamanilua`, `addViaOnline`, `downloadGameFix`, `requestDGameToken`, `isMassiveOnlineApp`, `isDGameApp`, `hasluaForApp`, `list_lua_apps`, `removeViamanilua`, `RefreshSteamClient`, `GetLocalLibrary`), manifest versions (`CheckManifestVersions`, `GetManifestVersionLookup`, `GetManifestVersionSelection`, `ApplyManifestVersion`, `OpenManifestPatchNotes`), health/OST (`RunHealthCheck`, `UpdateHealthFile(s)`, `health_manifest`, `InstallOpenSteamTool`, `GetOpenSteamToolStatus`, `CheckForPluginUpdates`/`ApplyPluginUpdate`/`GetUpdateStatus`), online files (`checkOnlineAvailability`, `checkPublicOnlineAvailability`, `checkGameFixAvailability`, `requestOnlineFile`, `online markers`, `checkOnlineFilesInstalled`, `removeOnlineFiles`, `CheckOnlineFileHealth`, `ShowOnlineFileWarning`), tools (`ActivateFixConnection`, `ActivateAllDonuvoGames`, `FixGameNotShowing`), MugiPlay (`GetMugiPlayDashboard`, `SendMugiPlayFriendRequest`, `RespondMugiPlayFriendRequest`, `RecordMugiPlayGame`).

**Download engines — bottom layer is one generated PowerShell worker:**
- `spawn_background_worker(appid, worker_type, config)` writes `worker_<appid>.ps1` + `config_<appid>.json` + `status_<appid>.json` in backend dir; types `manilua|online|d-games|game-fix`. Health-ping of `{ONLINE}/api/health` first; backslash-normalizes paths; self-invoked hidden via `ffi.C.WinExec` or `utils.exec`.
- **Unified/`addViamanilua`:** `GET api/plugin/game/{appid}?dtype=1|2` (d_type 1 = Lua-only, 2 = Manifest). Body: raw `.lua` → `config/lua` (or `config/stplug-in` if `lua_save_location` contains "stplug"); PK-zip → extract to Lua dir; `dtype=2` additionally routes `.manifest` into **Steam root `depotcache` (active)** + `config/depotcache` (persistent backup).
- **`addViaOnline` (online-fix.me):** availability check → metadata `plugin/online-files/{appid}` (`rarUrl|url`, `rarSize|fileSize`, `fileType` zip|rar, `rarPassword|password`, `unrarAvailable|unrarUrl`) → GET archive (600 s timeout, redirects) → unrar.exe fetched from server → **`unrar x -p"online-fix.me" -o+ -y`** or `Expand-Archive` → flatten single-folder + merge-move into game dir.
- **`requestDGameToken` (D-Games Denuvo fixes):** `POST d-games/{appid}/request {}` (api_key + online_key) → `downloadUrl` → worker downloads token zip → extract to game dir with unique `temp_token_<nonce>` staging (busy-status dedup per appid).
- **`downloadGameFix`:** `GET plugin/game-fix/{appid}/download` with `fix_nonce` (avail check first, `available:false` handled).
- **`isMassiveOnlineApp`:** `GET plugin/massive-online/{appid}` → `{massiveOnline|isMassiveOnline}` flag.
- Shared error handling: 401/403/429 bodies decoded first for `daily_limit`-code (does NOT kill the key); 429 = "out of points, weekly reset, get premium or epic"; 401/403 otherwise = wipe key (`requiresOnlineKey`); `hwid_mismatch`/`device_blocked`/`invalid_key`/`missing`/`online_key_banned` → auth-failed classification.

**Manifest persistence + restore guard (Steam eats active manifests — Mugi defends):**
- `get_depotcache_path` = **Steam root `depotcache` only** (config/depotcache was inactive staging — Steam only consumes root).
- `start_manifest_restore_guard`: hidden PS1 + `FileSystemWatcher` on root depotcache (mutex `Local\MugiManifestRestoreGuard`, filter `*.manifest*`), restore-on-delete from config/depotcache, 5 s rescan fallback while `steam` process alive.

**Health pipeline + self-repair (persisted updater — the pattern to mirror):**
- `health_manifest` = `GET /api/plugin/health-manifest` — **unauthenticated** → `{requiredPluginVersion, pluginDownloadUrl, files:[{fileKey,label,filename,sha256,downloadUrl}]}`; `fileKey=opensteamtool` = bundle `OpenSteamTool.dll + dwmapi.dll + xinput1_4.dll`, verified against `health_opensteamtool.sha256` marker.
- `RunHealthCheck`: millennium dir + `wsock32.dll` presence; Mugi plugin version vs required; enabled in `millennium\config\config.json` `plugins.enabledPlugins`; per-file sha256 vs manifest.
- `UpdateHealthFile(s)`: stage → **SHA-256 verify** (reject on mismatch) → for DLLs/OST bundle, kick a **detached replacer** that `Stop-Process steam/steamwebhelper`, copies staged files, writes marker, **restarts steam.exe** → success = "File verified. Steam is restarting."
- `run_powershell_hidden`: VBS + `WScript.Shell` `WinExec(…, 0)`; **detached mode registers a ONE-SHOT ScheduledTask `MugiHealthUpdate-<ts>-<rnd>`** (Interactive principal, RunLevel **Limited**, trigger once at +1 day, self-unregisters, self-deleting PS1) — survives Steam closing without admin.
- `InstallOpenSteamTool`: `https://mugi.store/download/ost.zip` → hidden PS1 expand into Steam root (validates subset of the bundle); legacy v4 path = `opst4.zip` + `install.ps1` fallback (runs elevated).

**Tool actions:** `ActivateFixConnection` — downloads `CloudRedirectCLI.exe` from GitHub **`Selectively11/CloudRedirect`** releases, runs `/stfixer` (visible console). `ActivateAllDonuvoGames` — `GET premium/activate-all/activate` → `.cmd` run **elevated** (`Start-Process -Verb RunAs`). `FixGameNotShowing` — `powershell … "irm steam.run | iex"`.

**Manifest versioning UI:** `POST manifest-versions/check {appid,depotid}` → job; UUID poll `GET manifest-versions/jobs/{uuid}` (36-char, hex+dash validated); `GetManifestVersionSelection` parses existing `setManifestid(depot,"…")` from the Lua file by regex; `ApplyManifestVersion` strips/replaces `setManifestid(depot,"…")` line with `.mugi-version-backup` rollback copy.

**Web dashboard SPA (`index-C3RiPVX_.js`, axios `/api` base + withCredentials):** routes — `/plugin/access`, `/plugin/api-key`, `/plugin/api-key/bindings/{id}` (`PATCH {nickname}` | `{mugiPlayEnabled}` | `{blocked}`, `DELETE` unlink), `/plugin/api-key/reset-binding`, `/plugin/cloud`, `/plugin/cloud/devices/{id}` (`{cloudEnabled}`), `/plugin/discord/check`, `/plugin/join-clan`, `/plugin/leave-clan`, `/api/auth/discord/callback`, `/api/version/desktop` (desktop updater), `/api/mugi-windows`, `/products`. Tier enum `free=0, nitro=1, premium=2, epic=3`; Discord guild-gated (`requiresGuild`, `discordInGuild`, `discordLinked`); Epic = 5 HWID **"Linked devices"** slots (auto-listed when plugin runs; block-first-then-remove semantics; nickname per device).

**Key endpoints (confirmed live):**
- `GET /api/plugin/health-manifest`, `/api/plugin-page`; `POST plugin/online-files/authenticate`, `plugin/massive-online/{appid}`, `plugin/online-files/{appid}/availability|request`, `plugin/game-fix/{appid}/download`, `d-games/{appid}|/request`, `manifest-versions/check|jobs/{uuid}`, `mugiplay|friends/request|friends/{id}/{action}|activity`, `plugin/api-key/bindings/{id}` PATCH/DELETE, `plugin/cloud/devices/{id}`.
- Installer: `install.ps1` + `millennium.ps1` + `/download/{plugin.zip, ost.zip, cloud_redirect.dll, opensteamtool.toml, MugiLauncher.exe}`.

**Tier/quota/hostage (rev 7 confirmed):** Free = **Discord guild hostage** (leaving deletes key); nitro/mtag hostage tiers; weekly quotas (manual/online/token) + daily usage; clan +10 weekly points; HWID binding + 24 h reset cooldown; guest checkout; Epic lifetime $49.99 (unlimited Denuvo tokens, 5 PCs).

**MugiCloud** (per-key, not per-PC): `usedBytes/quotaBytes/baseBytes/bonusBytes/bonusExpiresAt`; 500 MB free/key; +1 GB/yr = $9.99; buy-again-extends; "delete everything" confirm. R2 resold ~667x.

**MugiPlay (rev 7 — FINAL, both surfaces, the earlier "NOT social" is dead):**
- **Plugin side (social discovery, in `webkit.js`/`index.js`):** Steam top-nav tab **COMMUNITY → "MUGIPLAY"** (DOM text rewrite + class added). Setting `enable_mugiplay` (backend default `true`, but frontend cache default `false`; en.json hint: *"Show the MugiPlay tab and share future game additions with accepted Mugi friends."*). Dashboard = `GET mugiplay` → `{profile{tier:epic|premium, friendId, name, discordLinked, avatarUrl/avatarProxyUrl}, friends[], requests[], activity[]}`; friend-add input `MUGI-ABCD-2345` (maxLength 18); feed dedups by appid, contributors per game, self-first; **one-click library add via `addViamanilua`**, gated by `GetLocalLibrary()` (local Lua apps). **The record hook (exact):** after a successful `addViamanilua`, `if (enable_mugiplay) await RecordMugiPlayGame({appid, gameName})` → `POST mugiplay/activity`.
- **Dashboard side (per-device recording switch):** every Epic-key HWID binding has a `mugiPlayEnabled` toggle — *"Disable MugiPlay to stop recording new library additions from that device."* So: bindings are the **recording authority**, the plugin tab is the **consumption surface**.
- Social endpoint set: `SendMugiPlayFriendRequest` (`POST mugiplay/friends/request {friendId}`), `RespondMugiPlayFriendRequest` (`POST mugiplay/friends/{id}/(accept|decline|remove)`), `RecordMugiPlayGame` (`POST mugiplay/activity {appid,gameName}`). All require online key (`requiresOnlineKey:true` when missing).

**Trust signals:** Gridinsoft 1/100 (phishing), 8–10 blacklists, unsigned single-Cloudflare-domain distro, Discord OAuth = sole identity, destructive "reset" wipes Steam root + force-kills Steam mid-session, `cloud_redirect.dll` ships with no hash check, installer version skew (distro 5.2.0 vs MugiLauncher 2.3.0 RE sample).

## 2. Manifest & fix providers

- opensteamtool: `manifest.opensteamtool.com/{gid}`
- wudrm: `gmrc.wudrm.com/manifest/{gid}` -> plain digit string
- steamrun: `manifest.steam.run/api/manifest/{gid}` -> `{"content":"<gid>"}`
- Ryuu generator: `generator.ryuu.lol` — `/api/download/{appid}` (X-Auth-Key, `file_type=lua|manifest|zip`, `branch`), `/request`, `/requestupdate`, `/requestbranch` (crowdsourced dumps + pool-account model; Discord login gate).
- perondepot: `api.perondepot.xyz/all/` autoindex, `[appid]_Name.rar`, pw `online-fix.me`, appid from name prefix
- hubcapmanifest.com (paid, already in app) | GitHub mirror `qwe213312/k25FCdfEOoEJ42S6` | own `gabluchi-manifests`
- `ryuu_index.json` -> 0 hits, unverifiable (drop or replace)

## 3. Pin authority & depot internals (OST/BetterSteamTools source truth)

- `.acf` is **state**, never authority.
- Real pin = (a) `depotcache/<depot>_<gid>.manifest` (protobuf magics `0x71F617D0` / `0x1F4812BE` / `0x32C415AB`), (b) Lua `setManifestid(depot, gid, size)` pin (**Active vs Commented**), (c) hook-time `BuildDepotDependency` patching `DepotEntry.{DepotId, AppId, ManifestGid, ManifestSize}`. Denuvo/fix slots `forceLocked`.
- Depot keys: hook `LoadDepotDecryptionKey` at client load (no key file on disk). File fallback: `depotID;hexKey` temp -> `DepotDownloaderMod.exe -app <appid> -depot <depotid> -manifest <gid> -depotkeys <keys> -manifestfile <depotcache>/<depot>_<gid>.manifest -dir <out> -max-downloads 32 [-validate]` + `LogOnAnonymous()`.
- AppTicket: SteamDRM off-by-four forge from Steam's local ConfigStore ticket; `extract_tickets` on a title-owning machine -> AppTicket/ETicket hex -> credential store; Denuvo needs explicit ETicket.
- OST log modules: manifest / decryptionkey / keyvalue / richpresence / achievement / steamid.

## 4. GabLuchi repo audit (from full git tree)

- Services: UnlockerService 31 KB, HttpServerService 30 KB (RPC 127.0.0.1:6767), PluginInstallerService 22 KB, + AuthService, LicenseService, HardwareAppIdService, CloudRedirectService, GameHealthService, BackgroundHealthScanner, DefenderService, ManifestDownloader, ManifestPreCacheService, HubcapService, OnlineFixService, CrackFixService, MultiplayerFixService, GitHubFixService, FixRepository, CefInjectorService, SteamApiCheckBypassService, AnalyticsService, LobbyBrowserService, ConnectRelayService.
- Hardcoded chain: steamcmd.net v1/info; GitHub mirror `qwe213312/k25FCdfEOoEJ42S6`; hubcapmanifest.com; raw IP `167.235.229.108` (Ryuu/Luie); perondepot; online-fix.me; CrakFiles -> pixeldrain; sushi repo; gabluchi-fixes index.json.
- Bugs: buzzheavier IDs routed to pixeldrain API (wrong API); `OnlineFixEntry.SizeBytes` always 0; GitHubFixService TTL 5 min vs 24 h elsewhere; 3x duplicated apply pipeline; raw-IP defaults unstable; 7za path mismatch.
- Docs staleness: online.md / multiplayer-storage.md claim unshipped features (Health Check IS shipped); MugiPlay wrong in v5.0.0 breakdown; RE binary is 2.3.0 not 5.x.

## 5. Reference repos

- OpenSteam001/OpenSteamTool · OpenSteam001/steam-monitor (pattern) · BetterSteamTools (git.lua.tools) · ToxcGang/OpenLuaTools · ToxcGang/OpenSteamToolPlugin · swwayps/luatools-moon · XingDG/Luatools · madoiscool/LuaTools (C# mirror) · SCPLEGION/Millenium-steamtools-ryuu · ZiggyMar/SteamUnlock · SteamAutoCracks/Steam-API-Check-Bypass · Rattpak/CEG-Anti-Tamper-Analysis · Hegxib/SteamTools-Deep-Analyze · mugi.store.

## 6. Supply-chain warnings

- SteamTools ecosystem trojanized: `xinput1_4.dll` remote loader (AES-256-CBC `HttpLoadDLL` from CDNs, machine-hash obfuscation). Never copy. F-6 is the defense.
- Mugi: unsigned distro + no hash check on `cloud_redirect.dll`. F-6 does the opposite.

## 7. OST source-level RE (rev 3 round — source fetches)

- AppTicket off-by-four forge (`src/Utils/Tickets/AppTicket.cpp`): source = local ConfigStore cached ticket `kLocalAppTicketSourceAppId=7`; signed body (size−128) + 4 forged AppId bytes + 128-byte sig; `totalSize` = size−4; layout `[u32 size][u32 version][u64 steamId]`; `kSteamIdTicketMinimumSize=16`.
- **Forge exact offsets (rev 4, from source):** `AppTicket.h` — `kAppTicketSteamIdOffset=8`, `kAppTicketAppIdOffset=16`, `kAppTicketSignatureSize=128`; `AppTicketSource {CredentialStoreOnly, ForgeOnly, CredentialStoreThenForge}`. Forge math: physical = `source.size()+4` (signed body + 4 AppId bytes + sig tail); reported `totalSize = physical−4 = source.size()`; `appIdOffset = totalSize−128`; `signatureOffset = appIdOffset+4`; `steamIdOffset=8`; `signatureSize=128`. Credstore path: `signatureOffset = *((u32*)ticket.data())` (first u32); gated by `HasDepot(appId)`.
- **Lua registered surface (rev 4, `LuaConfig.cpp`):** exactly 8 active funcs — `addappid, addtoken, setmanifestid, http_get, http_post, setappticket, seteticket, setstat` (case-insensitive via named lowercase reg + `_G` `__index` metatable). `pinApp` defined, **NOT registered**. `addtoken(appid,"dec-u64")`, `setmanifestid(depot,"gid")` forces size 0, setApp/ETicket appid+hex→credstore, `setStat(appid,"steamid-u64")`.
- **Credential persistence (rev 4, `Windows/SteamCredentialStore.cpp`):** `HKCU\Software\Valve\Steam\Apps\{appid}` → `SteamID`(REG_SZ)/`AppTicket`(REG_BINARY)/`ETicket`(REG_BINARY); `HKCU\Software\Valve\Steam\ActiveProcess` → `ActiveUser`+`Universe` (Public/Beta/Internal/Dev) via `GetActiveUser`.
- **Denuvo auth module (rev 4, PR #109/#121/#128/#140 — PR "#148" does NOT exist; studied from source).** `src/Pipe/Features/DenuvoAuth/DenuvoAuth.cpp` + `ProtectionScan.{cpp,h}`:
  - Scan: legacy 5 sections (`.arch .srdata .xpdata .xdata .xtls`) + `DENUVO` string; high-version fast path = OEP section only, `DODENUVO` bytes `48 B9 44 4F 44 45 4E 55 56 4F`.
  - Pipe stages `None/Authorizing/EndAuthorization`, `kEndDenuvoVerificationHandshake=2`, per-pipe auth window (`IsAuthorizedPipe`/`CanUseAuthorizedIdentity`), `GetActiveUser(accountId, universeName)`. PR #121: scan main exe always (not only ≥80 MB modules). PR #128: fix SteamID offset in `AppTicket.cpp` (Token Error). PR #140: auth path engages when signature scan misses but ETicket injected.
- **`tools/extract_tickets` (rev 4, harvest-CLI recipe):** x64; SteamPath from `HKCU\Software\Valve\Steam`; load real `steamclient64.dll` (`SetDllDirectoryA`+`LOAD_WITH_ALTERED_SEARCH_PATH`); `CreateInterface("SteamClient023")`; `CreateSteamPipe`+`ConnectToGlobalUser`; **set `SteamAppId`/`SteamGameId` env BEFORE client init**; ownership sync via `STEAMAPPTICKET_INTERFACE_VERSION001::GetAppOwnershipTicketData`; encrypted async via `SteamUser023::RequestEncryptedAppTicket` + `SteamUtils010::IsAPICallCompleted`/`GetAPICallResult` (cb 100+54, 15 s bound); emits `<appid>\appticket.bin|eticket.bin|tickets.txt`. Universe `k_EUniversePublic=1,Beta=2,Internal=3,Dev=4`.
- **Manifest provider engine (rev 4, `ManifestClient.{h,cpp}`):** `kProviders[]` = `opensteamtool → https://manifest.opensteamtool.com/%llu` (plain uint64, default) | `wudrm → http://gmrc.wudrm.com/manifest/%llu` (plain digit) | `steamrun → https://manifest.steam.run/api/manifest/%llu` (`{"content":"<u64>"}`). Providers map **GID → request code** (NOT GID → manifest). Fetch priority: Lua `fetch_manifest_code_ex(appId,depotId,gid)` → `fetch_manifest_code(gid)` → active provider; Lua returns digit-string (double-precision >2^53). New provider = one table row.
  - **Live probe (2026-09-19):** wudrm & steamrun returned identical codes for identical input both ways → **mirrored backend**; `manifest.opensteamtool.com/{gid}` returns HTML boilerplate today, not a plain digit. "3 providers" ≈ 1–2 real legs.
- **Full hook inventory (rev 5, source fetch — the complete OST hook surface):**
  - `Hooks_Decryption.cpp`: `ConfigStoreGetBinary` — captures local ConfigStore (`g_pConfigStoreLocal` once), depot-key injection (`...\<DepotId>\DecryptionKey` → memcpy from LuaConfig, returns key size), `ReadConfigStoreLocalBinary(keyName)` via original. **Ticket cache source corrected: `GetCacheAppOwnershipTicket(appId)` reads `apptickets\{appId}` from session ConfigStore in-memory (NOT registry).** Registry = write-target (`setAppTicket/ETicket/SteamID`) and `CredentialStoreThenForge` priority source.
  - `Hooks_Manifest.cpp`: `BuildDepotDependency` post-call patch `DepotEntry.{ManifestGid,ManifestSize}` (size=0 → keep original), CUtlVector iteration.
  - `Hooks_Package.cpp`: `kInjectedPackageId=0`, `kInjectedPkgAccessToken=10660652434190618804ull`; `CheckAppOwnership` hook: `HasDepot(appId,false)` → owned (`ExistInPackageNums>1`) `MarkOwned`, else inject `PackageId=0,Released,OwnsLicense=true,bFreeLicense=false`; `InitFakeLicenseOnce` grows AppIdVec with `GetAllDepotIds()`, `MarkLicenseAsChanged(0,true)`+`ProcessPendingLicenseUpdates`. `NotifyLicenseChanged` handler.
  - `Hooks_Misc.cpp`: `GetAppIDForCurrentPipe` capture→`g_steamEngine`; `SpawnProcess` int3-trap → `pGameID.SetAppID(480)` + `g_OnlineFixRealAppId`; `OptedInMask` 480→real; overlay CGameID rewrite→real; `GetAppDataFromAppInfo` capture.
  - `Hooks_IPC.cpp` (+ISteamUser/ISteamUtils): `IPCProcessMessage` hook; handshake→`pipe->m_clientPID`; `ResolveDispatch` = interface+funcHash lookup, gated `LuaConfig::HasDepot(ResolveAppId())`; pre→orig→post. Handlers: `GetSteamID` (DenuvoAuth-window), `GetAppOwnershipTicketExtendedData` (CredentialStoreOnly in window else ForgeOnly; kOnlineFixAppId→real; sets pTicket/piAppId/piSteamId/piSignature/pcbSignature), `RequestEncryptedAppTicket` (PendingAPICalls record), `GetEncryptedAppTicket` (cache serve + buffer grow), `GetAppID` (480→real), `GetAPICallResult` (callback table → EncryptedAppTicketResponse OK).
  - **`Hooks_NetPacket.cpp` (16 handler namespaces — the big one):** send `BBuildAndAsyncSendFrame` + recv `RecvPkt` pooled replacement (8×64 KB pool). eMsg send: 151 service-job (FNV1a of `target_job_name`) → `GetUserStats`, `GetManifestRequestCode` (manifest-code wire replay), `Cloud.*` (CloudRedirect host, pass-through `Cloud.SignalAppExitSyncDone#1`/`ClientConflictResolution#1`, `g_SuppressSend` drops frame), 8903 PICS access-token, 742/5410 games-played OnlineFix, 7501 RP-upload track, 818 get-userstats send, 5466 store-userstats2. eMsg recv: 147 service-job → `NotifyRunningApps`, `GetUserStats` resp, `GetManifestRequestCode` resp; 819 stats-response replace; 9406 family-sharing null-body; 766 persona-state replace (RP spoof); 5527 commented (migrated to IPC).
  - `Hooks_KeyValues.cpp`: `ReadAsBinary` KV entry (`KeyValuesSystemSteam` from vstdlib_s64).
  - `Hooks_CallBack.cpp`: `SendCallbackToPipe` modifier dispatch.
  - `Hooks_SteamUI.cpp`: `FillInAppOverview`, `BuildCompleteAppOverviewChange`, `CSteamUIAppControllerRunFrame`.
- **steam-monitor branches (rev 6 — git branches, not dirs; default branch serves the protobuf archive):** 3 channels, all `steamclient|steamui/<sha256-of-dll>.toml`:
  - `pattern/` — 44 `steamclient/<sha>.toml` + 45 `steamui/<sha>.toml`: `[0x<funcHash>] name/rva/sig` (24 funcs in sample: BuildDepotDependency 0xC37F2D8E, LoadDepotDecryptionKey 0xB13C0C3F rva 0x5AE6D0, GetDecryptionKey 0xD51873C5, CheckAppOwnership 0x4B1B1D77, GetAppIDForCurrentPipe 0xA185DB47 w/ `??`, IPCProcessMessage 0xC3E20E29, GetPipeClient, PchMsgNameFromEMsg, SpawnProcess, SendCallbackToPipe, KeyValues_FindOrCreateKey/ReadAsBinary, OptedInMask, MarkLicenseAsChanged, etc.).
  - `ipc/`: per-interface `[IClientX] interface_id/vtable_rva`, per-method `method_index/funcHash/wrapper_rva/argc` (+fencepost) — **locked sample `0e68d65…toml` (rev 6):** `[IClientUser] interface_id=1 vtable_rva="0x12EBCE8"`; `[IClientUser.GetSteamID] method_index=10 funcHash=0xD6FC3200 wrapper_rva=0x782570 fencepost=0xD7058CA5 argc=0`; `GetAppOwnershipTicketExtendedData` idx105 hash 0xC7E71245 wrapper 0x74AAF0; `RequestEncryptedAppTicket` idx120 hash 0x25D6BB1D wrapper 0x83F9E0; `GetEncryptedAppTicket` idx121 hash 0xE0468CB4 wrapper 0x75D9B0; `[IClientUtils] interface_id=4 vtable_rva=0x12F1720`, `GetAppID` idx19 hash 0x09607EC4 wrapper 0x749850, `GetAPICallResult` idx24 hash 0x2D3D3947 wrapper 0x745690.
  - `protobuf/`: full client proto archive: `encrypted_app_ticket.proto` (`ticket_version_no=1, crc_encryptedticket=2, cb_encrypteduserdata=3, cb_encrypted_appownershipticket=4, encrypted_ticket=5`), `offline_ticket.proto`, `timedtrial.proto` (`TimedTrial.GetTimeRemaining/RecordPlaytime/ResetPlaytime`), ~90 steammessages/webuimessages protos.
- **LuaConfig state mechanics (rev 5):** per-file tracking — `g_fileDepots`, `g_fileManifestOverrides`, `g_fileParseSequence`, `g_depotRefCount`, `g_fileMtime`, `g_purchaseTime` (=max contributing file mtime); `RebuildManifestOverride` = highest parse-sequence file wins; pending add/remove consumed on license-change; `kDefaultStatSteamId=76561198028121353ULL` fallback; case-insens = lowercase `_G` + `g_func_registry` + `_G.__index` metamethod.
- Depot-key hook (`src/Hook/Hooks_Decryption.cpp`): `ConfigStoreGetBinary`, `k_EConfigStoreUserLocal`, key path `...\<DepotId>\DecryptionKey`, memcpy from LuaConfig; reads `apptickets\{appId}`.
- Manifest pin (`src/Hook/Hooks_Manifest.cpp`): `BuildDepotDependency` patches `DepotEntry.ManifestGid/ManifestSize`; size=0 keeps original.
- LuaConfig C++ API: HasDepot/IsOwned/MarkOwned/GetAllDepotIds/GetDecryptionKey/GetAccessToken/GetStatSteamId/pinApp/GetPurchaseTime/GetManifestOverrides(`ManifestOverride{gid,size}`)/ParseFile/UnloadFile/ParseDirectory/ReloadDirectories/TakePendingRemovals|Additions/HasManifestCodeFunc/CallManifestFetchCode{,Ex}.
- TOML schema: `[log]`, `[manifest url=opensteamtool|wudrm|steamrun]`, `[stats enable_api]`, `[lua paths]`, `[inject enabled/library_x64/_x86]`, `[cloud enabled/library]`, `[remote url_template={channel}/{component}/{sha256}]` — jsDelivr `OpenSteam001/steam-monitor@{channel}/{component}/{sha256}.toml`.
- Dual-channel pattern fetch (`RemoteToml.h`): pattern|ipc × steamclient|steamui, remote-then-cache; `PatternLoader.h`: metadata-before-hooks, RVA-first scans, `ReportMissingFunctions`.
- IPC (`IPCMessages.steamd`): InterfaceCall/FlushCallbacks/Destroy/Heartbeat/Handshake; OK=0x0B/Failed=0x0C; IClientUser=1, Friends=3, Utils=4, Apps=8, UserStats=11, AppManager=17, ConfigStore=18, GameCoordinator=19, UnifiedMessages=25; funcHash dispatch.
- Log tiers (`LogModules.def`): ipc, netpacket, manifest, keyvalue, decryptionkey, misc, achievement, pics, onlinefix, richpresence, package, steamui, pipe, platform.
- SteamMidra 16 Lua bindings: addappid/addtoken/setmanifestid/setappticket/seteticket/setstat/lchttpget/lchttppost/fetchmanifestcode/fetchmanifestcodeex/getcachedappticket/getdecryptionkey/seteticketurl/forcedenuvo/skipmanifestpin/addprocess. Registry forgery `HKCU\Software\Valve\Steam\Apps\{AppId}\AppTicket|ETicket` REG_BINARY; `steam.cfg ForBootStrapperInAll=Enable`; 42+128 ticket. **(rev 4: legacy/upstream-name compat — v1 OST `LuaConfig.cpp` registers only 8 of these.)**
- OST Denuvo surface (rev 4, PR #109/#121/#128/#140; "#148" was a ghost): pipe auth module (`DenuvoAuth.cpp`/`ProtectionScan.cpp`), not Web eticket-url. nonce etickets + `seteticketurl` + `forcedenuvo` + `addprocess` + eMsg 857→858 spoof remain **SteamMidra/marketing legacy** unless the fork reimplements them.
- **Pipeline activation (rev 6, `PipeManager.cpp`+`ProcessInspector.cpp`):** `OnHandshake(pipe)` → PipeKey(pid) → cached snapshot per `(pid,creationTime)` → env `SteamAppId`/`SteamGameId`/`SteamOverlayGameId` (+`&0xFFFFFF`) → `steamClientProcess` (name in `kSteamProcessNames`) / `likelyGameProcess` → `ResolveAppId()` → `trackedApp = HasDepot(appid,false)`, `owned = trackedApp && IsOwned` → **off-lock** `DenuvoAuth::Apply` then `Injection::Apply`. This is F-7's gate moment + F-1 trigger.
- **ProtectionScan exact (rev 6, `ProtectionScan.cpp`):** candidates exe/dll, `≥80 MB` (`kMinPackedModuleBytes=80*1024*1024`; PR #121 also always scans main exe), skip system/non-exe + 10 Steam-runtime names (steamclient(.64)/steam_api(.64)/tier0_s(.64)/vstdlib_s(.64)/gameoverlayrenderer(.64)); inGameTree by dir; sort exe→in-tree→size desc. Detect: OEP-section scan for `DODENUVO` bytes `48 B9 44 4F 44 45 4E 55 56 4F` first, else 5 legacy sections + `DENUVO` string. 8 MB chunked streaming BMH from disk.
- **Injection sidecar (rev 6, `Injection.cpp`+`Windows/RemoteProcess.cpp`):** config `{enabled, libraryX64, libraryX86}`; arch via IsWow64Process; only for `likelyGameProcess`, dedup by ProcessKey; classic `OpenProcess(CRT|QI|VM_OP|VM_WRITE|VM_READ)` → resolve remote `LoadLibraryW` (snapshot, forward-walk ≤8) → `VirtualAllocEx` RW → `WriteProcessMemory` → `CreateRemoteThread(LPW)` → wait 10 s → exit code≠0.
- **RemoteToml fetch (rev 6, `RemoteToml.cpp`):** sha256(dll) → cache `<steamroot>\opensteamtool\{channel}\{component}\{sha}.toml` → mirror chain (`remote.url_template` must contain `{channel}/{component}/{sha256}` else default GitHub-raw→jsDelivr); **404 = mirrors all-same-data → stop**; 200 → cache+return. F-2 port target.
- **NetPacket decode (rev 6):** delivery = pool rebuild (`ReplaceSendPacket`/`ReplaceRecvPacket`) OR in-place shrink (serialize into original pBody, `g_ResizedInPlace`, fake `m_cubData`) OR **carrier-borrow** (`TryInject`/`Drain`: swap `m_pubData`→prebuilt pkt for one `oRecvPkt` call, restore; cloud queue ≤64). GetUserStats: 818 forces `schema_local_version=-1` + rewrites `steam_id_for_user`; 819 clears stats+achievement_blocks, `eresult=1`, overlays CR achievement blocks (≤64) w/ recomputed `crc_stats` XOR. 147 resp: jobid_source→target correlator. 151 Manifest: async HTTP future, wait ≤12 s, inject `manifest_request_code` + eresult OK. AccessToken(8903): patch per-app access_token if HasDepot+has-token. OnlineFix(742/5410): `game_extra_info` = real name (via appinfo). **5527 commented in netpacket — migrated to IPC (`Hooks_IPC_ISteamUser::GetEncryptedAppTicketResponse`).**
- **IPC handlers decoded (rev 6, `Hooks_IPC_ISteamUser.cpp`):** `GetSteamID` post: noop unless `DenuvoAuth::IsAuthorizedPipe` else `GetSpoofSteamID(appId)`. `GetAppOwnershipTicketExtendedData` post: `cbMaxTicket<0` guard; onlinefix→real id; source = CredentialStoreOnly (auth window) else ForgeOnly; writes totalSize+offsets+ticket. `RequestEncryptedAppTicket` post: `PendingAPICalls::RecordEncryptedTicket(hAsyncCall,appId)`. `GetEncryptedAppTicket` post: serve cached credstore eticket; `EnsureBufferCapacity` (oEnsureBufferCapacity + `m_Put=cap`). `GetAppID` + `GetAPICallResult` (ISteamUtils) complete the loop.
- **Game name (rev 6, `Hooks_Misc::GetGameNameByAppID`):** NO HTTP — `oGetAppDataFromAppInfo(g_pCAppInfoCache, appId, "common/name", buf, 256)`; `"common/"` prefix ⇒ keyType=2 ⇒ tries `name_localized/<lang>` then falls back `name`; returns `strlen+1` / −1. Per-app cache. Feeds OnlineFix `game_extra_info` + 766 `game_name`.

## 8. GabLuchi backend anatomy (rev 3 round — live surface confirmed)

- Config keys (`Config.cs`): ManifestBackendBase `http://167.235.229.108` (UA `secretgoonpoon`, `/check_apis?appid=`); ApiBaseUrl `https://lua.tools` (`/api/dlc/info?appid&base=`, `/api/dlc/generate`, `/api/denuvo/listings`, `/api/denuvo/fixes?appid=`); AuthBackendBase `https://gabluchi-auth.freestuffsyeah65.workers.dev`; KeyCheckerBase `https://gabluchi-proxy.freestuffsyeah65.workers.dev` (`/activate`, `/manifest/{appid}?token=`, `/account`); DiscordClientId `1535479561158918216`. Unused/optional keys (defaults, wired but mostly empty): `DiscordInviteUrl=""`, `FixRepositoryPath=""`, `BackendDir=""`.
- AppConfig: HubcapBaseUrl `https://hubcapmanifest.com`; Steam storesearch/featuredcategories; hardware_appid list source repo `jsnli/steamappidlist` (`data/hardware_appid.json`); Steamless (atom0s) / CloudRedirect (Selectively11) repos; GH mirrors `ghproxy.net`/`ghfast.top`/`gh.ddlc.top`; PluginReleasesOwner `Gab-lutang/gabluchi-plugin`; steamcmd.net v1/info.
- License (`LicenseService.cs`): key `XXXXX-XXXXX-XXXXX-XXXXX`; machineId `SHA256(volumeSerial|MachineName|UserName)`; POST `{KeyCheckerBase}/activate {key,machineId,discordUserId}` → `{token}`; DPAPI token; `{KeyCheckerBase}/manifest/{appid}?token=` download gate (already exists).
- Auth (`AuthService.cs`): Discord OAuth PKCE S256 (code verifier → Base64Url(SHA256) challenge, `prompt=consent`), redirect `localhost:53789/callback` (HttpListener loopback, 5-min callback window), scopes `identify guilds`, `auth.dat` `%APPDATA%\GabLuchi`, http 90 s timeout, guest mode. Stored auth shape: `{Token, ExpiresAt, UserId, DisplayName, AvatarUrl}`; on startup `ValidateAsync(token, expiresAt)` against auth backend, expired/undecodable → session cleared, `AuthStateChanged` fired.
- Downloads dir: `InterimDownloadsFolder = %TEMP%\GabLuchi\downloads` (all hubcap/manifest/dlc downloads stage here; filenames from `Content-Disposition` or `{appid}.zip`/`{appid}.lua` fallback).
- Hubcap (`HubcapService.cs`): `/api/v1/user/stats?api_key=`; `/api/v1/status/{appid}` Bearer → `{status,manifest_file_exists}`; `/api/v1/manifest/{appid}?api_key=` → `{appid}.zip`; 401/429/404 error map.
- Local RPC `127.0.0.1:6767` (`HttpServerService.cs`): `/health/scan-status`, `/health/scan-all`, `/health/av-status`, `/health/{appid}`, `/health/{appid}/repair`, `/open-url`, `/api-list` + netsh urlacl.
- ConnectRelay (Go): ws lobby relay, 6-char codes, 5-min TTL, 10 req/min, 10 conns.
- SCPLEGION ryuu backend (`API_BASE http://scplegion.ovh:3000`, `backend/main.lua`): `/exists/{appid}` -> `{exists}`; `/sign/{appid}?ttl=` -> `{url|signed_url|download_url}`; `/file/{appid}` (signed-URL fallback, zip of manifests+lua); `/file/{appid}/metadata/files` -> `{files:{lua:[],manifest:[]}}`; `/userdata/save|get/{steamid}|delete` (per-SteamID manifest wishlist); `/stats/{appid}`; `/health`. Client installs: `.manifest` -> `steam/depotcache` + `config/depotcache`; `.lua`/`.json` -> `config/stplug-in`; `get_current_steam_sid` parses `loginusers.vdf` `MostRecent`.
- AppSettings surface (`AppSettings.cs`): `SteamPathOverride`, `SelectedMode`, `AutoUpdateApps`, `ManagePageSize`, `FixesPageSize`, `Language`, `HubcapApiKey`, `LicenseToken`, `LicenseMachineId`, `FastFetch`.
- Live probes (2026-09-19): `/api/denuvo/listings` public (tags `DenuvOwO`, `SteamTools Achievements Fix`, `voices38 (crack)`); `/api/dlc/info` + `/api/dlc/generate` anonymous → `Unauthorized`; `/api/denuvo/fixes?appid=1091500` → `No fixes found`; `hubcapmanifest.com` 405 on HEAD (GET only, Caddy).
- OnlineFixEntry: Source default `perondepot`, Password `online-fix.me`.

## 9. DenuvOwO HVB research (rev 3 round, rev 11 corrections inline)

- Hypervisor bypass (MKDev/Kirigiri): unsigned ring -1 driver (AMD `SimpleSvm.sys`, Intel `hyperkd.sys`), OS+Denuvo in guest VM; spoofs CPUID (clear hypervisor bit + zero vendor leaves), MSR, RDTSC/RDPMC (CounterUpdater), syscalls (LSTAR/EFER), EPT hooks (read-clean/exec-patched), RFLAGS.TF forwarding.
- **The other road — voices38 proper cracks (rev 9, 2026-09):** direct Denuvo strip, no driver/test-signing, security stays on; explicitly anti-hypervisor (same fragility/preservation critique as this guide Part 12). Denuvo filed §1201 suit against the alias 2026-09-14 (N.D. Cal, 26 games: Hogwarts Legacy, Black Myth: Wukong, Doom: The Dark Ages, Stellar Blade, Persona 3 Reload, Star Wars: Outlaws…; seeks injunction + unmasking via Reddit/Discord ID 1427416652684984397 / 7 Steam profiles); aliases replies "everything is fine". Civil suit, early stage — supply risk + demand shift onto HVB if releases stop. `HyperDevil` (DevilJinOfficial/HyperDevil) bundles both DenuvOwO and voices38 file sets with SHA-256 verification.
- Requires: VBS/HVCI/Credential Guard/System Guard/Windows Hello off; **DSE off — multiple paths (rev 11):** one-shot F7 boot (VBS.cmd route, V3 convenience), the package's built-in `0xDEADC0DE` UEFI-variable bypass (looks up `CiInitialize` in hal.dll/CI.dll, patches `g_CiEnabled`/`g_CiOptions` via `NtSetSystemEnvironmentValueEx`), or optional EfiGuard bootkit (Ring -2, disables DSE+PatchGuard at boot; cannot beat HVCI, needs Secure Boot off); PowerShell execution policy (script leaves it open — manual restore); BitLocker suspend.
- Hardcoded false-license expiry **= embedded ASN.1 fake-license certs `260226065959Z → 270226071959Z`, expires 2027-02-26 (rev 11 — was "~Feb 2027")**; fake Denuvo license blob written to `KIRIGIRI.bin` (6,247 B, next to exe, first launch, CPU-vendor-specific), served via `CreateFileW` IAT redirect on license ID `92346205896`; per-release files match exact build; AMD vs Intel = different drivers; older-Intel instability; Linux variant (LinUwUx) via cpuid_fault + Proton GE (Zen4 needs kernel ≥6.18, UMIP caveat).
- **6 published flaws = PoC not CVE (2026-04, rep `0-x-0-0/denuOwO-hypervisor-vulnerabilities`, no admin to trigger):** guest CR3 unvalidated (impersonation); write-what-where `CPUID[0x336933]`; exception-handler overwrite (ring3→ring0 hijack); VMLOAD/VMRUN race; unbounded CPUID/vmcall; KASLR leak `CPUID[0x41414141]`.
- Scene context 2026: by Apr 27 every Denuvo game bypassed; 2K/Irdeto added mandatory 14-day online check-ins (NBA 2K25/26, Marvel's Midnight Suns); Irdeto building a ring-0 countermeasure.
- Safer alternatives: voices38 proper cracks (RE: Requiem, Doom: The Dark Ages), offline activation tokens (hardware-bound, community Discord), traditional Steam-token emulation.

## 10. Millennium plugin API (docs.steambrew.app — live, matters for gabluchi-plugin)

- **LuaJIT backend now primary (Python deprecated).** Plugin API via `millennium` module:
  - Factory/sync: `ready()` (returns after loaded, ~10 s timeout), `version()`, `steam_path()`, `get_install_path()`, `get_plugin_logs()`.
  - Frontend injection: `add_browser_css(css)`, `add_browser_js(js)`, `remove_browser_module(name)`.
  - Settings/state: `get_user_settings()`, `set_user_settings_key(key, value)`.
  - Control/telemetry: `call_frontend_method(name, args)`, `change_plugin_status(plugin, on/off)`, `is_plugin_enabled(plugin)`, `__internal_get_build_date()`.
  - Helpers: `cmp_version(a, b)`.
- Promise-style model on factory funcs; plugin.json declares `backend`/`frontend` entrypoints. This is the target surface `gabluchi-plugin` scripts against (replaces the older Python examples floating around).

## 11. ryuu generator + SCPLEGION backend (route map)

- Ryuu (generator.ryuu.lol): `/api/download/{appid}` (X-Auth-Key header or `auth_key`, `file_type=lua|manifest|zip`, `branch`), `/request`, `/requestupdate`, `/requestbranch`; Discord OAuth login; crowdsourced dumps (users upload extracted tokens/keys) + pool-account mint.
- SCPLEGION (scplegion.ovh:3000, derives from ryuu): `/exists/{appid}`, `/sign/{appid}?ttl=`, `/file/{appid}`, `/file/{appid}/metadata/files`, `/userdata/save|get|delete`, `/stats/{appid}`, `/health`. Client installs `.manifest` to `steam/depotcache` + `config/depotcache`, `.lua/.json` to `config/stplug-in`; syncs user's manifest wishlist per SteamID; `get_current_steam_sid` reads `loginusers.vdf` `MostRecent`.

## 12. DenuvOwO/voices38 scene dossier (rev 11, 2026-09-20) — RD945 audit + Habr/CRACKLAB

**Sources:** `RD945/hypervisor-crack-audit` README — *"Comprehensive Security Audit for my college project. Resident.Evil.Requiem.HYPERVISOR.V2-KIRIGIRI"* (2026-03-04; independent student, passive static analysis only: PE headers, INF, ASCII/wide-string extraction, Ghidra 12.0.4 headless; 6 binaries, 1,066 functions, **41,644 lines** of pseudocode; nothing executed; package preview at file-me.top). Habr — *"DENUVO Hypervisor. How does it work?"* Blaukovitch (Max S. a.k.a. **Rose / Natasha 0x80000003, CRACKLAB team**), 2026-04-10, author spoke directly with Denuvo devs — publication approved by Denuvo GmbH (footer image). Full write-up gated at `cracklab.team/index.php?threads/1601/`.

**Verdicts (audit):** NO general malware evidence (no ransomware/spyware/keyloggers/miners/C2; Goldberg locked `offline=1`); zero network/injection/exfiltration imports in pseudocode (caveat: static can miss obfuscated/time-delayed). Risk table: **DSE+PatchGuard disable = CRITICAL**, BSOD **HIGH** (MKDEV's NFO: "not yet been tested for long term stability"), boot-chain modification + persistent security regression **HIGH**, HyperDbg physical-memory R/W **MEDIUM** (rootkit-class). Legal: copyright-infringement tool. Recommendation: build the chain from open-source components yourself instead of pre-built packages.

**Four privilege levels (audit):** Ring -2 EfiGuard (optional; `bootmgfw` `ImgpValidateImageHash`/`ImgpFilterValidationFailure`, `winload` `OslFwpKernelSetupPhase1`+`VbsPolicyDisabled` var, `ntoskrnl` PatchGuard: `KeInitAmd64SpecificState`, `CcInitializeBcbProfiler`, `ExpLicenseWatchInitWorker`, `g_PgContext`, `KiSwInterrupt`, `KiVerifyScopesExecute`, `KiMcaDeferredRecoveryService`; DSE: `SepInitializeCodeIntegrity`, `SeValidateImageData`, `SeCodeIntegrityQueryInformation`, `SetVariable` hook GUID `EfiGuardBackdoor`); Ring -1/+0 AMD **SimpleSvm.sys** (17,776 B, modified from tandasat — process-specific KUSER spoof added) vs Intel **hyperkd.sys** (11,632 B thin 26-fn shim) importing **hyperhv.dll** (modified HyperDbg, 762 funcs / 27,449 lines + `FUNC_*` scripting opcodes + `_PA` physical-address R/W) plus **hyperevade.dll**; Ring 3 **ColdClientLoader + detanup01's GBE fork** (stub overlay: `GameOverlayRenderer64.dll`), 5-layer stack = Steam + Denuvo + Capcom Anti-Tamper + VMProtect + SteamStub (RE Requiem AppId 3764200).

**KIRIGIRI.dll 32-bit x86, one export `KIRIGIRI` (1-byte RET no-op trigger), `entry()` in 9 steps:**
1. PEB spoof: `OSMajorVersion=16`, `OSBuildNumber=10`, `OSPlatformId=6`.
2. Syscall numbers read from ntdll (`NtQuerySystemInformation`, `NtQueryFullAttributesFile`) for direct syscalls.
3. Module cloaking: shadow copies of ntdll/kernel32/kernelbase/user32 in RWX `VirtualAlloc`; all three PEB module lists swapped so Denuvo's DLL walkers see clean unhooked copies.
4. Service install + DSE patch: CPUID leaf 0 vendor (AMD `0x444D4163`/Intel `0x6C65746E`), `CreateServiceW`/`StartServiceW` as **`denuvo_kirigiri`** (SERVICE_KERNEL_DRIVER, DEMAND_START); built-in DSE bypass maps hal.dll (build < `0x23F0`=9200) or CI.dll → `CiInitialize` → `g_CiEnabled`/`g_CiOptions` → `NtSetSystemEnvironmentValueEx` with UEFI var magic **`0xDEADC0DE`**; then launches `watchdog.exe -pid <pid>`.
5. Hypervisor registration: `CPUID(0x69696969)` = store process CR3; `CPUID(0x336933)` = game config data; `CPUID(0x1337)` = pass PID (CounterUpdater target); `Sleep(1000)`.
6. IAT hooks (6-byte push/ret): `NtQuerySystemInformation`/`ZwQuerySystemInformation` (hide hypervisor), `NtQueryFullAttributesFile` (hide crack files), `RegQueryValueExA` (returns error on `"HwProfileGuid"` — Denuvo HW fingerprinting), **`CreateFileW` → redirects Denuvo license-token read ID `"92346205896"` to `KIRIGIRI.bin`**.
7. Fake license creation: writes **`KIRIGIRI.bin`** (6,247 B/0x1867) next to the game exe, CPU-vendor-dependent blob. ASN.1 "Denuvo GmbH" cert issuer/subject strings embedded in both `.sys`; validity **`260226065959Z → 270226071959Z` (2026-02-26 06:59:59 → 2027-02-26 07:19:59 UTC)** = the license clock.
8. PEB unlinking of KIRIGIRI.dll itself (invisible to module enumeration).
9. Inline patches: `0x3CEE9` JMP (Capcom/SteamStub check over), `0x1B0` `MOV AL,1` (always-true), `0x9090` NOPs.

**Spoof surface:** CPUID leaf 0x1 ECX bit 31 (hypervisor-present) cleared + Family/Model `0x0A20F12`; CPUID leaf 0x0 vendor passthrough; fake brand **"DenuOvOwO CPU @ 1 337 GHz"** (audit) / **"DenuvOWO CPU @ 1337 GHz"** (Habr — proof results are fully forged); `CPUID(0x40000001)` vs `"Hv#0"` (`0x30237648`) = Hyper-V detection used by DenuvOwO itself; MSRs `0x4101/0x4104/0x4105` + `LSTAR` (0xC0000082); SVM via SVME `VM_CR` 0xC0010114; devirtualize = `CPUID(0x41414141)`; KUSER_SHARED_DATA CounterUpdater thread; `KdDebuggerNotPresent` ← TRUE; RDTSC/RDTSCP consistency; `\Callback\PowerState` devirt/revirt on sleep; `NtQuerySystemInformation(0xC4)` (=`SystemHypervisorDetailInformation`) abort if Hyper-V already owns VT-x/SVM; VMXON/VMLAUNCH (Intel) / VMRUN (AMD).

**Habr notes:** 2 pre-generated Denuvo tokens per CPU arch assigned by hypervisor choice, written to `.bin` on first launch, used by hypervisor as Denuvo tokens. Denuvo's own countermeasure requirements: reliable, not-easy-to-bypass, no performance hit, no kernel driver, no pain for legit buyers; candidates (EPT-hook detection via momo5502, DSE enforcement, timing) all spoofable → "all of Denuvo DRM checks can be bypassed this way (as of Feb 2026)". Post-OEP KUSER/WinAPI checks left as exercise. Author's stance: kernel-level cracks are risky (info-stealer potential); study rather than trust.

**Two generations (rev 11):** V2 (MKDEV-style, RE Requiem HYPERVISOR.V2-KIRIGIRI, pkg dated 2026-02-28; NFO `READNFO-MKDEV TEAM.txt`) = ColdClientLoader + GBE + EfiGuard-optional + `amd_ags_x64.org` proxy pattern; launch via `steamclient_loader_x64.exe`. V3 (DenuvOwO unified, cs.rin-approved 2026-03-18; FitGirl/AC Shadows 2026-05) = `hypervisor-launcher.exe` + `VBS.cmd` (v1.2, 2026-03-23) one-shot F7, no EfiGuard (removed — conflicts), token launch non-admin. **Per-release `DenuvOwO.nfo` is authoritative** for which runner to use.

**`amd_ags_x64.org` pattern:** original AMD GPU Services SDK renamed `.org`, replaced by a proxy `.dll` (static import → auto-loads bypass DLL at startup); `ColdClientLoader.ini` points `SteamClientDll`/`SteamClient64Dll` at GBE dlls; GBE `offline=1` (kills online token validation), fake user `KIRIGIRI`/`76561197960287930`, DLC pinned individually (`configs.app.ini`) not blindly unlocked.

**Cross-check (552 KB `/api/denuvo/listings` dump):** `DenuvOwO` tag = 3 games (`3321460` Crimson Desert ×3 fixes, `1285190` Borderlands 4, `2246340` Monster Hunter Wilds); `voices38 (crack)` = 29 games incl. `990080` Hogwarts Legacy, `3764200` RE Requiem, `1971870` MK1, `2161700` P3R, `2842040` Star Wars Outlaws; **only Crimson Desert dual-tagged** = canonical two-roads test case.

**Rev 11.1 addendum (2026-09-20) — post-lawsuit scene state:**

**Denuvo v. voices38** (Case `5:26-cv-10423`, N.D. Cal, filed 2026-09-14; TorrentFreak 09-16, heise 09-17, PC Gamer 09-17, VGC 09-18, Tom's Hardware 09-16, Kotaku 09-16). Two DMCA §1201 claims + jury demand; defendant "VOICES38, an unknown individual or entity, and DOES 1-10"; counsel **HG LAW LLP** (San Jose — Lundell/Samporna). Complaint names a Reddit account, one Discord user ID, **7 Steam profiles**; **no subpoena application yet** (Valve/Reddit/Discord disclosure is the likely next ladder step — Rockstar/Cyberleek pattern); injunction sought for *any past, present, or future game*; the 26-game list was **already out of date at filing** (P3R + SW Outlaws released days before). First US civil suit by Denuvo itself (Denuvo holds no game copyrights — standing via §1201); parent Irdeto's prior cracker action = **2018 Bulgaria criminal complaint vs Voksi** (hardware seized → retirement) — the worst-case template.

**voices38 activity:** after ~3-week break (last pre-burst release 08-11), dropped **five cracks in a single run 2026-09-06/07** — MK1, MGSV Complete, SW Outlaws, PoP Lost Crown, P3R (DSO/Gaming, CrackRelease 09-06; Nydus 09-07). Post-lawsuit reply on r/CrackWatch: *"All is fine. Everything will continue as normal."* (7k upvotes). Onimusha: Way of the Sword (Capcom, 09-04): DenuvOwO HV bypass **day-one**, voices38 full crack still pending.

**Dual-road games (rev 11.1):** recent DenuvOwO bypasses — Echoes of Aincrad (07-10, day after launch, v1.0.4) and 007 First Light 1.0.5 (build 23685521, 06-16) — coincide with voices38 versions of the same games in the scene, **but the feed tags only one side each**. Tag ≠ method: always resolve the per-release NFO.

**Listings-API cadence correction (rev 11.1):** `/api/denuvo/listings` carries **no timestamp fields at all**; the only time-like datum is the Steam CDN cache-buster `?t=<unix>` on `header_image` (a store-artifact, **not a fix-release date**). The 09-20 dump is byte-identical to the 09-20-01:23 dump (sha256 `98fc7ddc…`; newest CDN stamps 08-11 voices38 / 08-05 DenuvOwO) — but a quiet feed **cannot be read as a quiet scene**; derive cadence from scene news (DSO/Gaming, CrackWatch, isitcracked, Nydus), never from this API.

**Rev 11.2 addendum (2026-09-20) — lua.tools fix-download route (HAR-confirmed):**

**Contract:** `GET /api/denuvo/download?fix={fixUUID}&slot=fix` is real and session-gated; anonymous → 401 `{"error":"Unauthorized"}`. Legacy `lua.tools/denuvo/{appid}.zip` → 308 → `/fixes/…` 404; `lua.tools/api/denuvo/fix…` 404. Auth = Supabase: cookies `sb-db-auth-token.0` (base64 JSON: access_token JWT, refresh_token, expires_in 3600) + `.1`; app equivalence = decoded access_token as `Authorization: Bearer` (cookie fallback).

**Payload:** 200 `{"url":"<pre-signed Cloudflare R2>"}`; bucket `luatools-fixes.13d54514354ec5524add35e991bc8ae7.r2.cloudflarestorage.com`, key `/denuvo/{appid}/{uuid}/fix.zip`; `X-Amz-Expires=120`; `filename="{appid}.zip"` (capture example: `/denuvo/3321460/d94a3342-…/fix.zip`).

**Manifest separate & public:** `167.235.229.108/{appid}` → PK zip of `{appid}.lua` (addappid/setManifestid), no auth; `check_apis?appid=` → `{"Luie":"available","Ryuu":"available"}`. `lua.tools/api/manifest/check|download` → 401 "Login required" (irrelevant — Ryuu source is public).

**Implication:** Rev 10's "no fix route anywhere" is incorrect; bug = missing client call (`DenuvoDownloadResponse` unused, `RunDownload` manifest-fallthrough). Fix spec `PATCH.md §5`; 25/day = soft anti-scrape counter, not hard quota; auth mode = shared ref-token session (rotate the token shared in chat).