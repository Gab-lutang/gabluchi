<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="GabLuchi.Views.OlderVersionView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:res="clr-namespace:GabLuchi.Resources" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}">
  <Grid Name="Root">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
      <StackPanel MaxWidth="760" Margin="8,16,16,8">
        <TextBlock FontSize="22" FontWeight="Bold" Text="{x:Static res:Strings.OlderVersion_Title}" />
        <TextBlock Margin="0,2,0,0" FontSize="13" Foreground="#9ca3af" Text="Download specific depot versions using DepotDownloaderMod." />

        <!-- .NET 9 Status -->
        <Border Margin="0,18,0,0" Padding="14" Background="#0DFFFFFF" BorderBrush="#1FFFFFFF" BorderThickness="1" CornerRadius="10">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Margin="0,0,10,0" VerticalAlignment="Center" Symbol="Info24" />
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
              <TextBlock FontSize="13" FontWeight="SemiBold" Text="DepotDownloaderMod Status" />
              <TextBlock Margin="0,2,0,0" FontSize="12" Foreground="#9ca3af">
                <TextBlock.Style>
                  <Style TargetType="TextBlock">
                    <Setter Property="Text" Value="{x:Static res:Strings.OlderVersion_CheckDotNet}" />
                    <Style.Triggers>
                      <DataTrigger Value="True" Binding="{Binding IsToolPresent}">
                        <Setter Property="Text" Value="Tool: Found" />
                      </DataTrigger>
                    </Style.Triggers>
                  </Style>
                </TextBlock.Style>
              </TextBlock>
            </StackPanel>
            <ui:Button Grid.Column="2" VerticalAlignment="Center" Content="{x:Static res:Strings.OlderVersion_CheckDotNet}" Command="{Binding CheckDotNetCommand}" Icon="{ui:SymbolIcon ShieldTask24}" />
            <ui:Button Grid.Column="3" Margin="8,0,0,0" VerticalAlignment="Center" Content="{x:Static res:Strings.OlderVersion_DownloadNet9}" Command="{Binding DownloadDotNet9Command}" Icon="{ui:SymbolIcon Globe24}" Visibility="{Binding IsDotNet9Available, Converter={StaticResource InverseBoolToVis}}" />
          </Grid>
        </Border>

        <!-- Input Form -->
        <Border Margin="0,12,0,0" Padding="18" Background="#0DFFFFFF" BorderBrush="#1FFFFFFF" BorderThickness="1" CornerRadius="10">
          <StackPanel>
            <TextBlock FontSize="15" FontWeight="SemiBold" Text="Download Depot" />

            <!-- App ID -->
            <TextBlock Margin="0,12,0,4" FontSize="12" Foreground="#9ca3af" Text="{x:Static res:Strings.OlderVersion_AppId}" />
            <ui:TextBox Text="{Binding AppId, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Depot ID -->
            <TextBlock Margin="0,12,0,4" FontSize="12" Foreground="#9ca3af" Text="{x:Static res:Strings.OlderVersion_DepotId}" />
            <ui:TextBox Text="{Binding DepotId, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Manifest ID -->
            <TextBlock Margin="0,12,0,4" FontSize="12" Foreground="#9ca3af" Text="{x:Static res:Strings.OlderVersion_ManifestId}" />
            <ui:TextBox Text="{Binding ManifestId, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Depot Key (optional) -->
            <TextBlock Margin="0,12,0,4" FontSize="12" Foreground="#9ca3af" Text="Depot Key (optional)" />
            <ui:TextBox Text="{Binding DepotKey, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Destination -->
            <TextBlock Margin="0,12,0,4" FontSize="12" Foreground="#9ca3af" Text="{x:Static res:Strings.OlderVersion_DestDir}" />
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <ui:TextBox Grid.Column="0" Text="{Binding DestDir, UpdateSourceTrigger=PropertyChanged}" />
              <ui:Button Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center" Content="..." Command="{Binding BrowseDestCommand}" />
            </Grid>

            <!-- Buttons -->
            <StackPanel Margin="0,18,0,0" Orientation="Horizontal">
              <ui:Button HorizontalAlignment="Stretch" Content="{x:Static res:Strings.OlderVersion_OpenSteamDb}" Command="{Binding OpenSteamDbForAppCommand}" Icon="{ui:SymbolIcon Globe24}" />
              <ui:Button Margin="10,0,0,0" HorizontalAlignment="Stretch" Content="{x:Static res:Strings.OlderVersion_Download}" Command="{Binding DownloadCommand}" Icon="{ui:SymbolIcon ArrowDownload24}" IsEnabled="{Binding CanDownload}" />
              <ui:Button Margin="10,0,0,0" HorizontalAlignment="Stretch" Content="Cancel" Command="{Binding CancelDownloadCommand}" Icon="{ui:SymbolIcon Dismiss24}" Visibility="{Binding IsDownloading, Converter={StaticResource BoolToVis}}" />
            </StackPanel>

            <!-- Progress -->
            <StackPanel Margin="0,14,0,0" Visibility="{Binding IsDownloading, Converter={StaticResource BoolToVis}}">
              <ui:ProgressRing Height="4" IsIndeterminate="{Binding IsProgressIndeterminate}" />
              <Grid Margin="0,6,0,0">
                <ProgressBar Height="6" Maximum="100" Value="{Binding Progress}" />
              </Grid>
              <TextBlock Margin="0,6,0,0" FontSize="12" Foreground="#9ca3af" Text="{Binding StatusMessage}" />
            </StackPanel>

            <!-- Status (when not downloading) -->
            <TextBlock Margin="0,10,0,0" FontSize="12" Foreground="#9ca3af" Text="{Binding StatusMessage}" Visibility="{Binding IsDownloading, Converter={StaticResource InverseBoolToVis}}" />
          </StackPanel>
        </Border>

        <!-- Help -->
        <Border Margin="0,12,0,0" Padding="14" Background="#0DFFFFFF" BorderBrush="#1FFFFFFF" BorderThickness="1" CornerRadius="10">
          <StackPanel>
            <TextBlock FontSize="13" FontWeight="SemiBold" Text="How to use" />
            <TextBlock Margin="0,6,0,0" FontSize="12" Foreground="#9ca3af" TextWrapping="Wrap" Text="1. Enter your App ID and click 'Open SteamDB' to browse depot versions." />
            <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="#9ca3af" TextWrapping="Wrap" Text="2. On SteamDB, go to the Depots tab and find the depot ID and manifest ID you want." />
            <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="#9ca3af" TextWrapping="Wrap" Text="3. Enter the depot ID and manifest ID above, then click Download." />
            <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="#9ca3af" TextWrapping="Wrap" Text="4. Depot key is optional — only needed for private depots." />
            <TextBlock Margin="0,8,0,0" FontSize="11" Foreground="#6b7280" TextWrapping="Wrap" Text="Requires .NET 9 runtime. Download from: dotnet.microsoft.com/download/dotnet/9.0" />
          </StackPanel>
        </Border>
      </StackPanel>
    </ScrollViewer>
  </Grid>
</UserControl>
