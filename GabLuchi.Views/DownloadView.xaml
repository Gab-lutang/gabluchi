<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="GabLuchi.Views.DownloadView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:res="clr-namespace:GabLuchi.Resources" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:views="clr-namespace:GabLuchi.Views" xmlns:vm="clr-namespace:GabLuchi.ViewModels" TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <DataTemplate x:Key="DiffRowTemplate">
        <Button Margin="0,0,0,2" Padding="6,4" HorizontalContentAlignment="Stretch" Background="#00FFFFFF" BorderThickness="0" Cursor="Hand" ToolTip="{x:Static res:Strings.Confirm_OpenSteamDb}" Command="{Binding DataContext.OpenSteamDbCommand, ElementName=Root}" CommandParameter="{Binding}">
          <Control.Template>
            <ControlTemplate TargetType="{x:Type Button}">
              <Border Name="Bd" Padding="{TemplateBinding Control.Padding}" Background="{TemplateBinding Control.Background}" CornerRadius="6">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <StackPanel>
                    <TextBlock FontSize="12.5" Foreground="#e5e7eb" TextTrimming="CharacterEllipsis" Text="{Binding Title}" />
                    <TextBlock Margin="0,1,0,0" FontSize="10.5" Foreground="#6b7280" Text="{Binding Meta}" />
                  </StackPanel>
                  <Border Grid.Column="1" Margin="8,1,0,0" Padding="5,1" VerticalAlignment="Top" Background="#2a2440" CornerRadius="4" Visibility="{Binding IsDlc, Converter={StaticResource BoolToVis}}">
                    <TextBlock FontSize="9.5" FontWeight="SemiBold" Foreground="#c4b5fd" Text="DLC" />
                  </Border>
                  <Border Grid.Column="1" Margin="8,1,0,0" Padding="5,1" VerticalAlignment="Top" Background="#3a3320" CornerRadius="4" Visibility="{Binding IsShared, Converter={StaticResource BoolToVis}}">
                    <TextBlock FontSize="9.5" FontWeight="SemiBold" Foreground="#d6b36b" Text="{x:Static res:Strings.Confirm_Shared}" />
                  </Border>
                </Grid>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="Bd" Property="Border.Background" Value="#14FFFFFF" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Control.Template>
        </Button>
      </DataTemplate>
      <DataTemplate x:Key="FeaturedCardTemplate">
        <Button Width="190" Margin="0,0,10,0" Padding="0" HorizontalContentAlignment="Stretch" Background="#00FFFFFF" BorderThickness="0" Cursor="Hand" Command="{Binding DataContext.SelectFeaturedCommand, ElementName=Root}" CommandParameter="{Binding}" ToolTip="{Binding Name}">
          <Control.Template>
            <ControlTemplate TargetType="{x:Type Button}">
              <Border Name="Bd" Background="#0DFFFFFF" BorderBrush="#14FFFFFF" BorderThickness="1" CornerRadius="8">
                <StackPanel>
                  <Border CornerRadius="7,7,0,0">
                    <Image Height="89" Stretch="UniformToFill" Source="{Binding Image}" />
                  </Border>
                  <TextBlock Height="34" Margin="8,6,8,8" FontSize="12" Foreground="#d1d5db" TextTrimming="CharacterEllipsis" TextWrapping="Wrap" Text="{Binding Name}" />
                </StackPanel>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="Bd" Property="Border.Background" Value="#1AFFFFFF" />
                  <Setter TargetName="Bd" Property="Border.BorderBrush" Value="#33a78bfa" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Control.Template>
        </Button>
      </DataTemplate>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Grid Name="Root">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
      <StackPanel MaxWidth="640" Margin="8,16,8,24">
        <TextBlock FontSize="22" FontWeight="Bold" Text="{x:Static res:Strings.Add_Title}" />
        <TextBlock Margin="0,4,0,16" FontSize="13" Foreground="#9ca3af" Text="{x:Static res:Strings.Add_Subtitle}" />
        <ui:TextBox PlaceholderText="{x:Static res:Strings.Add_SearchPlaceholder}" Icon="{ui:SymbolIcon Search24}" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
        <Border Margin="0,6,0,0" Background="#14141c" BorderBrush="#14FFFFFF" BorderThickness="1" CornerRadius="10" Visibility="{Binding IsResultsOpen, Converter={StaticResource BoolToVis}}">
          <ItemsControl ItemsSource="{Binding SearchResults}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Background="#00FFFFFF" BorderThickness="0" Cursor="Hand" Command="{Binding DataContext.SelectResultCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                  <Grid Margin="2">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border Width="60" Height="28" CornerRadius="4">
                      <Border.Background>
                        <ImageBrush Stretch="UniformToFill" ImageSource="{Binding Icon}" />
                      </Border.Background>
                    </Border>
                    <TextBlock Grid.Column="1" Margin="10,0" VerticalAlignment="Center" FontSize="13" TextTrimming="CharacterEllipsis" Text="{Binding Name}" />
                    <TextBlock Grid.Column="2" VerticalAlignment="Center" FontSize="11.5" Foreground="#6b7280" Text="{Binding AppId}" />
                  </Grid>
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </Border>
        <StackPanel Margin="0,18,0,0" Visibility="{Binding ShowFeatured, Converter={StaticResource BoolToVis}}">
          <StackPanel Visibility="{Binding HasTopSellers, Converter={StaticResource BoolToVis}}">
            <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="#d1d5db" Text="{x:Static res:Strings.Add_Featured_TopSellers}" />
            <ScrollViewer Margin="0,8,0,0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" PreviewMouseWheel="FeaturedStrip_PreviewMouseWheel">
              <ItemsControl ItemTemplate="{StaticResource FeaturedCardTemplate}" ItemsSource="{Binding TopSellers}">
                <ItemsControl.ItemsPanel>
                  <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                  </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
              </ItemsControl>
            </ScrollViewer>
          </StackPanel>
          <StackPanel Margin="0,16,0,0" Visibility="{Binding HasNewReleases, Converter={StaticResource BoolToVis}}">
            <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="#d1d5db" Text="{x:Static res:Strings.Add_Featured_NewReleases}" />
            <ScrollViewer Margin="0,8,0,0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" PreviewMouseWheel="FeaturedStrip_PreviewMouseWheel">
              <ItemsControl ItemTemplate="{StaticResource FeaturedCardTemplate}" ItemsSource="{Binding NewReleases}">
                <ItemsControl.ItemsPanel>
                  <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                  </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
              </ItemsControl>
            </ScrollViewer>
          </StackPanel>
        </StackPanel>
        <Border Margin="0,14,0,0" Background="#0DFFFFFF" BorderBrush="#12FFFFFF" BorderThickness="1" CornerRadius="8" Visibility="{Binding HasDetails, Converter={StaticResource BoolToVis}}">
          <StackPanel>
            <Border MaxHeight="180" CornerRadius="8,8,0,0">
              <Image Stretch="UniformToFill" Source="{Binding Details.HeaderImage}" />
            </Border>
            <StackPanel Margin="12,8,12,10">
              <StackPanel Orientation="Horizontal">
                <TextBlock FontSize="14.5" FontWeight="SemiBold" Text="{Binding Details.Name}" />
                <Border Margin="8,0,0,0" Padding="6,1.5" VerticalAlignment="Center" Background="#26a78bfa" BorderBrush="#40a78bfa" BorderThickness="1" CornerRadius="4" Visibility="{Binding Details.IsDlc, Converter={StaticResource BoolToVis}}">
                  <TextBlock FontSize="10.5" FontWeight="SemiBold" Foreground="#a78bfa" Text="DLC" />
                </Border>
              </StackPanel>
              <TextBlock Margin="0,3,0,0" FontSize="11.5" Foreground="#6b7280">
                <Run Text="{Binding Details.AppId, Mode=OneWay}" /> <Run Text=" · " /> <Run Text="{Binding GenresText, Mode=OneWay}" /></TextBlock>
              <TextBlock FontSize="11" Foreground="#4b5563" Text="{Binding Details.ReleaseDate, StringFormat={x:Static res:Strings.Add_Released}}" Visibility="{Binding Details.ReleaseDate, Converter={StaticResource NullToCollapsed}}" />
            </StackPanel>
          </StackPanel>
        </Border>
        <Grid Margin="0,12,0,0" Visibility="{Binding HasDetails, Converter={StaticResource BoolToVis}}">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <StackPanel VerticalAlignment="Center">
            <TextBlock FontSize="13" FontWeight="Medium" Text="{x:Static res:Strings.Add_FastFetch}" />
            <TextBlock FontSize="11" Foreground="#6b7280" Text="{x:Static res:Strings.Add_FastFetch_Hint}" />
          </StackPanel>
          <ui:ToggleSwitch Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding FastFetch, Mode=TwoWay}" />
        </Grid>
        <ui:Button Margin="0,14,0,0" HorizontalAlignment="Stretch" Appearance="Primary" Command="{Binding FetchCommand}" Icon="{ui:SymbolIcon ArrowDownload24}" Visibility="{Binding HasDetails, Converter={StaticResource BoolToVis}}">
          <ContentControl.Content>
            <Grid>
              <TextBlock Text="{x:Static res:Strings.Add_Fetch}" Visibility="{Binding IsChecking, Converter={StaticResource InverseBoolToVis}}" />
              <TextBlock Text="{x:Static res:Strings.Add_Checking}" Visibility="{Binding IsChecking, Converter={StaticResource BoolToVis}}" />
            </Grid>
          </ContentControl.Content>
        </ui:Button>
        <TextBlock Margin="0,12,0,0" FontSize="13" Foreground="#f87171" TextWrapping="Wrap" Text="{Binding Error}" Visibility="{Binding Error, Converter={StaticResource NullToCollapsed}}" />
        <Border Margin="0,12,0,0" Padding="12,9" CornerRadius="8" Visibility="{Binding HasInstallResult, Converter={StaticResource BoolToVis}}">
          <FrameworkElement.Style>
            <Style TargetType="{x:Type Border}">
              <Setter Property="Background" Value="#1422c55e" />
              <Setter Property="BorderBrush" Value="#4022c55e" />
              <Setter Property="BorderThickness" Value="1" />
              <Style.Triggers>
                <DataTrigger Value="True" Binding="{Binding InstallFailed}">
                  <Setter Property="Background" Value="#14f0a868" />
                  <Setter Property="BorderBrush" Value="#40f0a868" />
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </FrameworkElement.Style>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon VerticalAlignment="Center">
              <FrameworkElement.Style>
                <Style TargetType="{x:Type ui:SymbolIcon}">
                  <Setter Property="ui:IconElement.Foreground" Value="#22c55e" />
                  <Setter Property="ui:SymbolIcon.Symbol" Value="CheckmarkCircle24" />
                  <Style.Triggers>
                    <DataTrigger Value="True" Binding="{Binding InstallFailed}">
                      <Setter Property="ui:IconElement.Foreground" Value="#f0a868" />
                      <Setter Property="ui:SymbolIcon.Symbol" Value="Warning24" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </FrameworkElement.Style>
            </ui:SymbolIcon>
            <TextBlock Grid.Column="1" Margin="10,0" VerticalAlignment="Center" FontSize="13" TextWrapping="Wrap" Text="{Binding InstallStatus}" />
            <ui:Button Grid.Column="2" Content="{x:Static res:Strings.Add_Reveal}" Command="{Binding RevealInstalledCommand}" Icon="{ui:SymbolIcon FolderOpen24}" Visibility="{Binding InstallFailed, Converter={StaticResource InverseBoolToVis}}" />
          </Grid>
        </Border>
        <ItemsControl Margin="0,12,0,0" ItemsSource="{Binding Sources}" Visibility="{Binding HasSources, Converter={StaticResource BoolToVis}}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Border Margin="0,0,0,8" Padding="12,9" Background="#08FFFFFF" BorderBrush="#12FFFFFF" BorderThickness="1" CornerRadius="8">
                <StackPanel>
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                      <TextBlock FontSize="13.5" FontWeight="Medium" Text="{Binding DisplayName}" />
                      <ui:SymbolIcon Margin="6,0,0,0" FontSize="15" Foreground="#eab308" Symbol="Key24" ToolTip="{x:Static res:Strings.Add_RequiresOwnKey}" Visibility="{Binding NeedsKey, Converter={StaticResource BoolToVis}}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="0,0,8,0" VerticalAlignment="Center" Orientation="Horizontal">
                      <Border Padding="6,2" Background="#1422c55e" BorderBrush="#3022c55e" BorderThickness="1" CornerRadius="4" Visibility="{Binding StatsText, Converter={StaticResource NullToCollapsed}}">
                        <TextBlock FontSize="11" FontWeight="SemiBold" Foreground="#22c55e" Text="{Binding StatsText}" />
                      </Border>

                    </StackPanel>
                    <Border Grid.Column="2" Margin="0,0,8,0" Padding="6,2" VerticalAlignment="Center" BorderThickness="1" CornerRadius="4" Visibility="{Binding ShowStatus, Converter={StaticResource BoolToVis}}" BorderBrush="{Binding Converter={StaticResource StatusToBrush}, Path=Status}">
                      <TextBlock FontSize="10.5" FontWeight="SemiBold" Foreground="{Binding Status, Converter={StaticResource StatusToBrush}}" Text="{Binding StatusLabel}" />
                    </Border>
                    <ui:Button Grid.Column="3" Margin="0,0,8,0" Padding="8,5" Content="{x:Static res:Strings.Add_Discord}" FontSize="11.5" ToolTip="{x:Static res:Strings.Add_JoinDiscord}" Command="{Binding OpenDiscordCommand}" Visibility="{Binding DiscordUrl, Converter={StaticResource NullToCollapsed}}" />
                    <ui:Button Grid.Column="4" Appearance="Primary" Content="{x:Static res:Strings.Add_Download}" Command="{Binding DownloadCommand}" Icon="{ui:SymbolIcon ArrowDownload24}" Visibility="{Binding CanDownload, Converter={StaticResource BoolToVis}}" />
                    <TextBlock Grid.Column="4" VerticalAlignment="Center" FontSize="11.5" Foreground="#a78bfa" Text="{x:Static res:Strings.Add_NeedsHubcapKey}" ToolTip="{x:Static res:Strings.Add_NeedsHubcapKey_Tip}" Visibility="{Binding IsLocked, Converter={StaticResource BoolToVis}}" />
                  </Grid>
                  <ProgressBar Margin="0,8,0,0" Maximum="100" IsIndeterminate="{Binding IsProgressIndeterminate}" Visibility="{Binding IsDownloading, Converter={StaticResource BoolToVis}}" Value="{Binding Progress, Mode=OneWay}" />
                </StackPanel>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <TextBlock Margin="0,24,0,0" FontSize="13" FontWeight="SemiBold" Foreground="#d1d5db" Text="{x:Static res:Strings.Add_AlreadyHaveFiles}" />
        <views:DropZone Margin="0,8,0,0" DataContext="{Binding Drop}" />
      </StackPanel>
    </ScrollViewer>
    <Grid Background="#99000000" Visibility="{Binding IsConfirmingOverwrite, Converter={StaticResource BoolToVis}}">
      <Border Width="460" MaxHeight="560" Margin="24" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#1c1c22" BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="12">
        <Grid Margin="20">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <TextBlock FontSize="17" FontWeight="Bold" TextWrapping="Wrap" Text="{Binding ConfirmTitle}" />
          <TextBlock Grid.Row="1" Margin="0,4,0,0" FontSize="12" Foreground="#9ca3af" Text="{x:Static res:Strings.Confirm_ReviewChanges}" />
          <ScrollViewer Grid.Row="2" Margin="0,14,0,0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
              <TextBlock FontSize="12.5" Foreground="#6b7280" TextWrapping="Wrap" Text="{Binding ConfirmNoChanges}" Visibility="{Binding ConfirmNoChanges, Converter={StaticResource NullToCollapsed}}" />
              <StackPanel Visibility="{Binding HasDiffAdded, Converter={StaticResource BoolToVis}}">
                <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="#34d399" Text="{Binding DiffAdded.Count, StringFormat={x:Static res:Strings.Confirm_Adding}}" />
                <ItemsControl Margin="0,6,0,0" ItemTemplate="{StaticResource DiffRowTemplate}" ItemsSource="{Binding DiffAdded}" />
              </StackPanel>
              <StackPanel Margin="0,10,0,0" Visibility="{Binding HasDiffRemoved, Converter={StaticResource BoolToVis}}">
                <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="#f0a868" Text="{Binding DiffRemoved.Count, StringFormat={x:Static res:Strings.Confirm_Removing}}" />
                <ItemsControl Margin="0,6,0,0" ItemTemplate="{StaticResource DiffRowTemplate}" ItemsSource="{Binding DiffRemoved}" />
              </StackPanel>
            </StackPanel>
          </ScrollViewer>
          <StackPanel Grid.Row="3" Margin="0,16,0,0" HorizontalAlignment="Right" Orientation="Horizontal">
            <ui:Button Margin="0,0,8,0" Content="{x:Static res:Strings.Mode_Cancel}" Command="{Binding CancelOverwriteCommand}" />
            <ui:Button Appearance="Primary" Content="{x:Static res:Strings.Confirm_Replace}" Command="{Binding ConfirmOverwriteCommand}" Icon="{ui:SymbolIcon ArrowDownload24}" />
          </StackPanel>
        </Grid>
      </Border>
    </Grid>
    <views:ConfirmOverlay DataContext="{Binding Drop}" />
  </Grid>
</UserControl>