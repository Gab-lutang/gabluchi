<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="GabLuchi.Views.OnboardingView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:res="clr-namespace:GabLuchi.Resources" xmlns:sys="clr-namespace:System;assembly=mscorlib" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:vm="clr-namespace:GabLuchi.ViewModels" xmlns:views="clr-namespace:GabLuchi.Views">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <Style x:Key="ChoiceButton" BasedOn="{StaticResource {x:Type ui:Button}}" TargetType="{x:Type ui:Button}">
        <Setter Property="MinWidth" Value="92" />
        <Setter Property="Margin" Value="6,0,0,0" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
      </Style>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Grid Background="#66000000">
    <Border MaxWidth="640" Margin="16" Padding="30,32" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#1c1c22" BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="14">
      <StackPanel>
        <Image Height="64" Margin="0,0,0,18" HorizontalAlignment="Center" Source="pack://application:,,,/Assets/gabluchi_logo.png" Stretch="Uniform" />
        <TextBlock FontSize="21" FontWeight="Bold" Text="{x:Static res:Strings.Onboarding_Title}" TextWrapping="Wrap" />
        <Border Height="1" Margin="0,24,0,0" Background="#1FFFFFFF" />
        <Border Margin="0,24,0,0" Padding="16,14" Background="#0D8B5CF6" BorderBrush="#338B5CF6" BorderThickness="1" CornerRadius="10" Visibility="{Binding IsGuest, Converter={StaticResource BoolToVis}}">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel VerticalAlignment="Center">
              <TextBlock FontSize="14" FontWeight="SemiBold" Text="{x:Static res:Strings.Settings_BrowsingAsGuest}" />
              <TextBlock Margin="0,2,0,0" FontSize="12" Foreground="#7c8593" Text="{x:Static res:Strings.Onboarding_SignIn_Blurb}" TextWrapping="Wrap" />
            </StackPanel>
            <ui:Button Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center" Appearance="Primary" Content="{x:Static res:Strings.Settings_SignInDiscord}" Command="{Binding SignInCommand}" Icon="{ui:SymbolIcon Person24}" IsEnabled="{Binding NotBusy}" />
          </Grid>
        </Border>
        <StackPanel Margin="0,24,0,0" Orientation="Horizontal" Visibility="{Binding IsGuest, Converter={StaticResource InverseBoolToVis}}">
          <ui:SymbolIcon VerticalAlignment="Center" FontSize="18" Foreground="#34d399" Symbol="CheckmarkCircle24" />
          <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Text="{x:Static res:Strings.Onboarding_SignedIn}" />
        </StackPanel>
        <TextBlock Margin="0,10,0,0" FontSize="12" Foreground="#f87171" TextWrapping="Wrap" Text="{Binding StatusLine}" Visibility="{Binding StatusLine, Converter={StaticResource NullToCollapsed}}" />
        <Grid Margin="0,24,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <StackPanel Margin="0,0,28,0" VerticalAlignment="Center">
            <TextBlock FontSize="14" FontWeight="SemiBold" Text="{x:Static res:Strings.Onboarding_Recommended_Title}" />
            <TextBlock Margin="0,2,0,0" FontSize="12" Foreground="#7c8593" Text="{x:Static res:Strings.Onboarding_Recommended_Detail}" TextWrapping="Wrap" />
          </StackPanel>
          <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Horizontal">
            <ui:Button Content="{x:Static res:Strings.Onboarding_Yes}" Command="{Binding SetApplyRecommendedCommand}">
              <ButtonBase.CommandParameter>
                <system:Boolean xmlns:system="clr-namespace:System;assembly=System.Private.CoreLib">True</system:Boolean>
              </ButtonBase.CommandParameter>
              <FrameworkElement.Style>
                <Style BasedOn="{StaticResource ChoiceButton}" TargetType="{x:Type ui:Button}">
                  <Style.Triggers>
                    <DataTrigger Value="True" Binding="{Binding ApplyRecommended}">
                      <Setter Property="ui:Button.Appearance" Value="Primary" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </FrameworkElement.Style>
            </ui:Button>
            <ui:Button Content="{x:Static res:Strings.Onboarding_No}" Command="{Binding SetApplyRecommendedCommand}">
              <ButtonBase.CommandParameter>
                <Boolean xmlns="clr-namespace:System;assembly=System.Private.CoreLib">False</Boolean>
              </ButtonBase.CommandParameter>
              <FrameworkElement.Style>
                <Style BasedOn="{StaticResource ChoiceButton}" TargetType="{x:Type ui:Button}">
                  <Style.Triggers>
                    <DataTrigger Value="False" Binding="{Binding ApplyRecommended}">
                      <Setter Property="ui:Button.Appearance" Value="Primary" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </FrameworkElement.Style>
            </ui:Button>
          </StackPanel>
        </Grid>
        <Grid Margin="0,22,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <StackPanel Margin="0,0,28,0" VerticalAlignment="Center">
            <TextBlock FontSize="14" FontWeight="SemiBold" Text="{x:Static res:Strings.Onboarding_Plugin_Title}" />
            <TextBlock Margin="0,2,0,0" FontSize="12" Foreground="#7c8593" Text="{x:Static res:Strings.Onboarding_Plugin_Detail}" TextWrapping="Wrap" />
          </StackPanel>
          <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Horizontal">
            <ui:Button Content="{x:Static res:Strings.Onboarding_Yes}" Command="{Binding SetInstallPluginCommand}">
              <ButtonBase.CommandParameter>
                <Boolean xmlns="clr-namespace:System;assembly=System.Private.CoreLib">True</Boolean>
              </ButtonBase.CommandParameter>
              <FrameworkElement.Style>
                <Style BasedOn="{StaticResource ChoiceButton}" TargetType="{x:Type ui:Button}">
                  <Style.Triggers>
                    <DataTrigger Value="True" Binding="{Binding InstallPlugin}">
                      <Setter Property="ui:Button.Appearance" Value="Primary" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </FrameworkElement.Style>
            </ui:Button>
            <ui:Button Content="{x:Static res:Strings.Onboarding_No}" Command="{Binding SetInstallPluginCommand}">
              <ButtonBase.CommandParameter>
                <Boolean xmlns="clr-namespace:System;assembly=System.Private.CoreLib">False</Boolean>
              </ButtonBase.CommandParameter>
              <FrameworkElement.Style>
                <Style BasedOn="{StaticResource ChoiceButton}" TargetType="{x:Type ui:Button}">
                  <Style.Triggers>
                    <DataTrigger Value="False" Binding="{Binding InstallPlugin}">
                      <Setter Property="ui:Button.Appearance" Value="Primary" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </FrameworkElement.Style>
            </ui:Button>
          </StackPanel>
        </Grid>
        <ui:Button Margin="0,28,0,0" Padding="0,10" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" Appearance="Primary" Content="{x:Static res:Strings.Onboarding_Go}" FontSize="14" Command="{Binding FinishCommand}" IsEnabled="{Binding NotBusy}" />
      </StackPanel>
    </Border>
  </Grid>
</UserControl>