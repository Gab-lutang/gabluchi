<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="GabLuchi.Views.PluginView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:res="clr-namespace:GabLuchi.Resources" xmlns:vm="clr-namespace:GabLuchi.ViewModels" TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}" xmlns:views="clr-namespace:GabLuchi.Views">
  <UserControl.Resources>
    <ResourceDictionary>
      <Style x:Key="CompRow" TargetType="{x:Type Border}">
        <Setter Property="Margin" Value="0,0,0,6" />
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="Background" Value="#0AFFFFFF" />
        <Setter Property="BorderBrush" Value="#14FFFFFF" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
      </Style>
      <Style x:Key="CompIcon" TargetType="{x:Type Border}">
        <Setter Property="Width" Value="34" />
        <Setter Property="Height" Value="34" />
        <Setter Property="Background" Value="#1aa78bfa" />
        <Setter Property="CornerRadius" Value="8" />
      </Style>
    </ResourceDictionary>
  </UserControl.Resources>
  <Grid Name="Root">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
      <StackPanel MaxWidth="1040" Margin="8,16,8,32" HorizontalAlignment="Center">
        <TextBlock FontSize="22" FontWeight="Bold" Text="{x:Static res:Strings.Plugin_Title}" />
        <TextBlock Margin="0,4,0,0" FontSize="13" Foreground="#9ca3af" Text="{x:Static res:Strings.Plugin_Subtitle}" TextWrapping="Wrap" />

        <Grid Margin="0,18,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="280" />
          </Grid.ColumnDefinitions>

          <StackPanel Margin="0,0,14,0">
            <Border Height="196" Background="#0DFFFFFF" BorderBrush="#1FFFFFFF" BorderThickness="1" CornerRadius="10" ClipToBounds="True">
              <Grid>
                <Image HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Opacity="0.14" Stretch="UniformToFill" IsHitTestVisible="False" Source="{Binding MemeUri, Converter={StaticResource ImagePathToSource}}" />
                <StackPanel Margin="22,26,22,18" VerticalAlignment="Bottom">
                    <StackPanel Orientation="Horizontal">
                      <Border Width="44" Height="44" VerticalAlignment="Center" Background="#1aa78bfa" CornerRadius="10">
                        <ui:SymbolIcon FontSize="20" Foreground="#a78bfa" Symbol="PuzzlePiece24" />
                      </Border>
                      <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                          <TextBlock FontSize="20" FontWeight="Bold" TextTrimming="CharacterEllipsis" Text="{x:Static res:Strings.Plugin_CardTitle}" />
                          <Border Margin="10,0,0,0" Padding="7,2" VerticalAlignment="Center" Background="#207c3aed" CornerRadius="4" Visibility="{Binding IsInstalled, Converter={StaticResource BoolToVis}}">
                            <TextBlock FontSize="10.5" FontWeight="SemiBold" Foreground="#c4b5fd" Text="{Binding InstalledVersion}" />
                          </Border>
                          <Border Margin="6,0,0,0" Padding="7,2" VerticalAlignment="Center" Background="#20f59e0b" CornerRadius="4" Visibility="{Binding UpdateAvailable, Converter={StaticResource BoolToVis}}">
                            <TextBlock FontSize="10.5" FontWeight="SemiBold" Foreground="#fbbf24" Text="{x:Static res:Strings.Plugin_Badge_UpdateAvailable}" />
                          </Border>
                          <Border Margin="6,0,0,0" Padding="7,2" VerticalAlignment="Center" Background="#2022c55e" CornerRadius="4" Visibility="{Binding ShowUpToDate, Converter={StaticResource BoolToVis}}">
                            <TextBlock FontSize="10.5" FontWeight="SemiBold" Foreground="#4ade80" Text="{x:Static res:Strings.Plugin_Status_UpToDate}" />
                          </Border>
                        </StackPanel>
                        <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="#94a3b8">
                          <Run Text="{Binding InstalledVersion}" /> <Run Text=" · " /> <Run Text="{Binding LatestVersion}" />
                        </TextBlock>
                      </StackPanel>
                    </StackPanel>
                  </StackPanel>
                </Grid>
            </Border>

            <Border Margin="0,12,0,0" Padding="16,13" Background="#0DFFFFFF" BorderBrush="#1FFFFFFF" BorderThickness="1" CornerRadius="10">
              <StackPanel>
                <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="#d1d5db" Text="{x:Static res:Strings.Plugin_Components_Header}" />

                <Border Margin="0,10,0,0" Style="{StaticResource CompRow}">
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border Style="{StaticResource CompIcon}">
                      <ui:SymbolIcon FontSize="18" Foreground="#a78bfa" Symbol="PuzzlePiece24" />
                    </Border>
                    <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                      <TextBlock FontSize="12.5" FontWeight="SemiBold" Foreground="#e5e7eb" Text="{x:Static res:Strings.Plugin_Row_Frontend}" />
                      <TextBlock Margin="0,2,0,0" FontSize="11" Foreground="#6b7280" Text="{Binding FrontendStatus}" />
                    </StackPanel>
                    <Grid Grid.Column="2" VerticalAlignment="Center">
                      <ui:SymbolIcon FontSize="16" Foreground="#4ade80" Symbol="CheckmarkCircle24" Visibility="{Binding FrontendInstalled, Converter={StaticResource BoolToVis}}" />
                      <ui:SymbolIcon FontSize="16" Foreground="#6b7280" Symbol="DismissCircle24" Visibility="{Binding FrontendInstalled, Converter={StaticResource InverseBoolToVis}}" />
                    </Grid>
                  </Grid>
                </Border>

                <Border Style="{StaticResource CompRow}">
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border Style="{StaticResource CompIcon}">
                      <ui:SymbolIcon FontSize="18" Foreground="#38bdf8" Symbol="ArrowSync24" />
                    </Border>
                    <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                      <TextBlock FontSize="12.5" FontWeight="SemiBold" Foreground="#e5e7eb" Text="{x:Static res:Strings.Plugin_Row_Loader}" />
                      <TextBlock Margin="0,2,0,0" FontSize="11" Foreground="#6b7280" Text="{Binding DllStatus}" />
                    </StackPanel>
                    <Grid Grid.Column="2" VerticalAlignment="Center">
                      <ui:SymbolIcon FontSize="16" Foreground="#4ade80" Symbol="CheckmarkCircle24" Visibility="{Binding DllOk, Converter={StaticResource BoolToVis}}" />
                      <ui:SymbolIcon FontSize="16" Foreground="#fbbf24" Symbol="Warning24" Visibility="{Binding DllOutOfDate, Converter={StaticResource BoolToVis}}" />
                      <ui:SymbolIcon FontSize="16" Foreground="#6b7280" Symbol="DismissCircle24" Visibility="{Binding DllNotInstalled, Converter={StaticResource BoolToVis}}" />
                    </Grid>
                  </Grid>
                </Border>

                <Border Style="{StaticResource CompRow}">
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border Style="{StaticResource CompIcon}">
                      <ui:SymbolIcon FontSize="18" Foreground="#a78bfa" Symbol="Games24" />
                    </Border>
                    <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                      <TextBlock FontSize="12.5" FontWeight="SemiBold" Foreground="#e5e7eb" Text="{x:Static res:Strings.Plugin_Row_Steam}" />
                      <TextBlock Margin="0,2,0,0" FontSize="11" Foreground="#6b7280" Text="{Binding SteamStatus}" />
                    </StackPanel>
                    <Grid Grid.Column="2" VerticalAlignment="Center">
                      <ui:SymbolIcon FontSize="16" Foreground="#4ade80" Symbol="CheckmarkCircle24" Visibility="{Binding SteamFound, Converter={StaticResource BoolToVis}}" />
                      <ui:SymbolIcon FontSize="16" Foreground="#6b7280" Symbol="DismissCircle24" Visibility="{Binding SteamFound, Converter={StaticResource InverseBoolToVis}}" />
                    </Grid>
                  </Grid>
                </Border>

                <Border Style="{StaticResource CompRow}">
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border Style="{StaticResource CompIcon}">
                      <ui:SymbolIcon FontSize="18" Foreground="#38bdf8" Symbol="Tag24" />
                    </Border>
                    <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                      <TextBlock FontSize="12.5" FontWeight="SemiBold" Foreground="#e5e7eb" Text="{x:Static res:Strings.Plugin_Row_Bridge}" />
                      <TextBlock Margin="0,2,0,0" FontSize="11" Foreground="#6b7280" Text="{Binding PortStatus}" />
                    </StackPanel>
                    <Grid Grid.Column="2" VerticalAlignment="Center">
                      <ui:SymbolIcon FontSize="16" Foreground="#4ade80" Symbol="CheckmarkCircle24" Visibility="{Binding PortBusy, Converter={StaticResource InverseBoolToVis}}" />
                      <ui:SymbolIcon FontSize="16" Foreground="#fbbf24" Symbol="Warning24" Visibility="{Binding PortBusy, Converter={StaticResource BoolToVis}}" />
                    </Grid>
                  </Grid>
                </Border>

                <Border Margin="0,6,0,0" Padding="10,8" Background="#141a78bf" CornerRadius="4" Visibility="{Binding MillenniumCoexisting, Converter={StaticResource BoolToVis}}">
                  <TextBlock FontSize="11.5" Foreground="#93c5fd" Text="{x:Static res:Strings.Plugin_Millennium_Coexisting}" TextWrapping="Wrap" />
                </Border>
              </StackPanel>
            </Border>
          </StackPanel>

          <Border Grid.Column="1" Padding="16" Background="#0DFFFFFF" BorderBrush="#1FFFFFFF" BorderThickness="1" CornerRadius="10">
            <StackPanel>
              <TextBlock FontSize="15" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Text="{x:Static res:Strings.Plugin_CardTitle}" />
              <TextBlock Margin="0,2,0,0" FontSize="11" Foreground="#6b7280">
                <Run Text="{Binding InstalledVersion}" /> <Run Text=" · " /> <Run Text="{x:Static res:Strings.Plugin_Tag_Loader}" />
              </TextBlock>
              <Border Margin="0,12,0,0" Padding="5,1.5" HorizontalAlignment="Left" Background="#22a78bfa" CornerRadius="3">
                <TextBlock FontSize="10" FontWeight="SemiBold" Foreground="#a78bfa" Text="{x:Static res:Strings.Plugin_Tag_Loader}" />
              </Border>
              <ui:Button Margin="0,10,0,0" MinWidth="140" HorizontalAlignment="Stretch" Command="{Binding InstallCommand}" Content="{Binding InstallButtonText}" IsEnabled="{Binding NotBusy}">
                <FrameworkElement.Style>
                  <Style BasedOn="{StaticResource {x:Type ui:Button}}" TargetType="{x:Type ui:Button}">
                    <Setter Property="ui:Button.Appearance" Value="Secondary" />
                    <Style.Triggers>
                      <DataTrigger Value="True" Binding="{Binding InstallIsPrimary}">
                        <Setter Property="ui:Button.Appearance" Value="Primary" />
                      </DataTrigger>
                    </Style.Triggers>
                  </Style>
                </FrameworkElement.Style>
              </ui:Button>
              <ui:Button Margin="0,8,0,0" HorizontalAlignment="Stretch" Content="{x:Static res:Strings.Plugin_Btn_CheckForUpdates}" Command="{Binding CheckForUpdatesCommand}" IsEnabled="{Binding NotBusy}" />
              <ui:Button Margin="0,8,0,0" HorizontalAlignment="Stretch" Content="{x:Static res:Strings.Plugin_Btn_Uninstall}" Command="{Binding UninstallCommand}" IsEnabled="{Binding CanUninstall}" Icon="{ui:SymbolIcon Delete24}" Appearance="Danger" Visibility="{Binding IsInstalled, Converter={StaticResource BoolToVis}}" />
              <TextBlock Margin="0,10,0,0" FontSize="11" Foreground="#6b7280" TextWrapping="Wrap" Text="{x:Static res:Strings.Plugin_Rail_Hint}" />
              <ProgressBar Margin="0,12,0,0" Height="4" Maximum="100" IsIndeterminate="{Binding IsProgressIndeterminate}" Value="{Binding Progress}" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />
              <TextBlock Margin="0,10,0,0" FontSize="12.5" Foreground="#f59e0b" TextWrapping="Wrap" Text="{Binding StatusLine}" Visibility="{Binding StatusLine, Converter={StaticResource NullToCollapsed}}" />
            </StackPanel>
          </Border>
        </Grid>

        <TextBlock Margin="2,14,0,0" FontSize="11.5" Foreground="#6b7280" Text="{x:Static res:Strings.Plugin_Footer}" TextWrapping="Wrap" />
      </StackPanel>
    </ScrollViewer>
  </Grid>
</UserControl>